# 更新日志

## Unreleased

- 新增「表达模式」常驻规则层（Response Mode，源自《提示处理.md》附件设计 + 业界对标综合）：新增 `docs/rules/response-modes.md`，定位为与角色（决策视角）、Skill（领域能力）正交的表达控制层——只控制思考深度、表达方式、输出粒度与输出结构，不改变任务目标、事实口径、证据标准、授权与安全边界，不新增流程或完成门禁。规则包含：所有回复默认生效的基准表达合同（结论优先、具体事实不空泛、流程/问题/代码三类结构模板、四类证据标签沿用 governance-core、只展开任务相关内容）；18 个模式目录按「问题理解（/brief、/explain、/explain-simple、/understand、/trace、/flow）、任务决策（/clarify、/design、/compare）、任务执行（/do、/debug、/refactor）、质量控制（/test、/review、/completeness）、交接与修饰（/handoff、/business、/deep）」分组，每条给触发信号与表达要求；与既有能力重叠的模式不复制内容、用绑定指针去重（/clarify→clarify-requirements Skill、/debug→systematic-debugging Skill + troubleshooting 规则、/test→test-rules、/review→review-report 六字段契约、/do→governance-core Fast Path）；激活方式为按任务类型自动选择 + 消息内显式 `/模式名`（单消息作用域、显式优先）+ 组合规则「一个主任务模式 + 至多一个表达修饰模式」，模式不隐式作用于子 Agent；保底条款对标 Claude Code Concise——错误报告、安全告警、授权确认请求、验证结果与证据标签任何模式下不可裁剪，交付物契约（delivery 三字段、review-report 六字段、clarify-requirements 提问结构）优先于模式，验证深度由风险档位决定与模式解耦，冲突解析顺序为「授权与安全边界 > 用户本轮显式指令 > 交付物契约 > 显式模式 > 自动模式 > 基准合同」；规则末尾绑定 `vibe-harness-response-modes` Eval 作为收敛观察入口。注册链：`manifests/rules.json`、`scripts/lib/rules-index.js` 治理组、`adapters/install-map.json` rules-core 条目、`docs/catalog.json` 与 `docs/README.md` 索引。AGENTS.md 渲染链：`scripts/lib/install-planner.js` 新增 `responseModeLine`（仅当计划含该规则文件时渲染，镜像 clarificationPostureLine 条件逻辑），codex/opencode/antigravity 三个指令模板加 `{{installedSurface.responseModeLine}}` 占位符，`scripts/lib/template-renderer.js` 补默认值，本仓库自装 AGENTS.md 按新渲染重放。评测：新增 `evals/suites/vibe-harness-response-modes.json`（EVAL-RESPMODE-001..006：通俗解释自动选 explain-simple、显式 /brief 结论优先、/trace+/explain-simple 组合上限、模式不改变验证与完成行为的反向断言、/brief 下错误/安全/确认信息保底、/review+/brief 六字段完整），`adapters/install-map.json` 补 evals-online 条目并经 `eval:sync` 投影到 `.agents/evals/suites/`，`manifests/capabilities.json` 新增 `response-mode-selection` 能力（generalize、evaluation required 绑定该 suite、profiles minimal/core/full/docs-only）。调研沉淀 `docs/inventory/response-mode-investigation.md`（Claude Code Output Styles、Cursor Custom Modes、Codex AGENTS.md+skills 双通道、aider chat modes 等机制对比与共识清单、对本设计的映射取舍）。验证：`pnpm check`（unit 149/149、component 249/249）、`pnpm skills:audit`（15 Skills、0 findings）、`pnpm eval:check`、`pnpm eval:replay`（overallScore 1、criticalPassRate 1）、`git diff --check`。指纹漂移为 config + rules 两组加 aggregate（config 归因 manifests/rules.json 与 manifests/capabilities.json，rules 归因 docs/rules/response-modes.md 新增、fileCount 45→46；hooks、skills 未动），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --confirm-reference-update --force` → `eval:sync --write` → `eval:replay --write` 再生成，共两轮（第二轮因规则文件补 suite 收敛绑定句），备份于 `.vibe-harness/backups/2026-09-18T01-36-*` 与 `.vibe-harness/backups/2026-09-18T01-38-*`。
- codebase-memory-mcp 规范整改与 0.9.0 → 0.11.0 升级：规则、Hook 判定、索引入口与新鲜度此前三处互相矛盾，改动前实测到四个根因。其一，规则引用的 `list_projects`/`index_status`/`detect_changes`/`trace_call_path` 在锁定的 0.9.0 MCP 面上并不存在（`tools/list` 只返回 8 个工具，`trace_call_path` 是早已更名的旧别名，现名为 `trace_path`），规则的第一步在任何会话里都无法执行。其二，`runtime/hooks/lib/read-only-commands.mjs` 对 MCP 工具名只按动词正则猜意图，`trace_path`（无读取动词）、`index_repository`、`index_status`、`detect_changes` 全被判为「不可判定」而要求 Execution Envelope，裸命令 `codebase-memory-mcp cli ...` 也整条被拒，语义图因此从未在真实会话里被调用。其三，索引缓存分叉：受管 MCP 块把 `CBM_CACHE_DIR` 指向项目内 `.vibe-harness/tool-state/...`，而上一轮人工「重建」写的是用户级缓存，MCP 读到的一直是旧图。其四，没有任何新鲜度信号：`index_status` 的 HEAD 是实时读工作区、`ready` 只说明图可读，`auto_index`/`auto_watch` 又被 provisioning 主动关闭，索引静默过期无人察觉。**行为变化**：受管 MCP 与 provisioning 的 `CBM_CACHE_DIR` 从项目内迁到用户私有目录（Windows 为 `%LOCALAPPDATA%\vibe-harness\codebase-memory-mcp\<project-slug>`，其他平台为 XDG cache 等价路径），`CBM_ALLOWED_ROOT`、`CBM_MEM_BUDGET_MB=2048`、`CBM_WORKERS=2` 与 `auto_index`/`auto_watch=false` 均保持不变——迁移理由是 0.11.0 会拒绝路径链上存在不受信身份写权限的缓存目录，`C:\` 默认继承 `Authenticated Users:(M)`，项目内缓存必然失败，而修 ACL 不是解法；新增诊断码 `CBM_CACHE_DIR_NOT_PRIVATE` 与 `INDEX_CACHE_RESET_REFUSED`，前者的前置校验只认 DACL 拒绝，其余 precheck 失败不再降级（索引阶段才是权威）。新增共享路径解析 `runtime/tools/codebase-memory-mcp/cache-path.mjs`，受管 MCP 块、`componentEnvironment`、runtime wrapper 与项目命令四处共用同一实现，MCP 查询与管理 CLI 因此必然指向同一张图；wrapper 新增 `project-root.mjs`（`git rev-parse --git-common-dir` 识别 linked worktree 并把索引/查询映射到主检出，`status` 返回 `sourceRoot`）、`index-state.mjs`（状态戳契约 `schemaVersion/project/rootPath/headSha/branch/indexedAt/mode/nodes/edges/cacheDir/runtimeVersion`），并在**两条入口**（MCP 与 CLI）的 `index_repository` 成功返回后写入 `.vibe-harness/tool-state/codebase-memory-mcp/index-state.json`——写戳失败只告警、不影响工具运行。新增项目命令子面 `node .agents/runtime/commands/run.mjs codebase-memory <status|refresh> --project . [--json]`：`status` 只读、返回 `fresh|stale|missing` 与依据（状态戳 headSha 比对、`indexed_at`、必要时 `check_index_coverage` 探针），`refresh` 在 `--write` 前保持 dry-run；两者成为规则与 `AGENTS.md` 的唯一推荐入口。`vibe-harness doctor` 与 `validate --project` 报告缓存位置、状态戳新鲜度与 `CODEBASE_MEMORY_CACHE_STALE_COPY`（陈旧副本只报告、不自动删除），`uninstall`/`rollback` 在项目 runtime 离开后清理该项目的私有缓存（fail-safe：runtime 仍在项目或计划未移除 runtime 时不清）。Hook 侧改为服务器 + 工具名的显式判定表（`MCP_TOOL_DECISIONS`）：13 个查询工具加 `index_repository`、`ingest_traces` 直接放行（后两者只重写工具自己的图缓存、可重复执行且不碰项目文件），`manage_adr` 按 workspaceWrite 需 Envelope，`delete_project` 保持高风险拒绝；未登记服务器仍回退动词正则，`classifyMcpToolName` 与 `execution-envelope.mjs` 共用 `mcpToolPolicy`。0.11.0 契约适配包括 CLI 默认输出由 JSON 变为人类可读 `key: value`、`--json` 才给 MCP 信封（`index_repository` 信封内是 JSON、`index_status` 信封内是文本），`parseCommandJson` 因此接受三种形态，`validateIndexStatus` 的字段校验由 `git.*` 改为 `indexed_at`，index/index-verify 阶段统一加 `--json`，`--repo-path`/`--repo_path` 兼容与 `--mode moderate --persistence false` 保留；版本抬升覆盖 `runtime/tools/codebase-memory-mcp/package.json`、`package-lock.json`、`pnpm-lock.yaml`、`environment.js`、`docs/specs/vibe-harness-tooling-modules-spec.md` 与 Windows 二进制摘要表（0.11.0 `7EDCD3807EBCFD85EC1968985964080F2589748DA2FC3C7CE9261EEBAB31FF04`），不做双版本并存。`docs/rules/codebase-memory-mcp.md` 按 0.11.0 实测 MCP 面（17 个工具）整体重写，明确「先 status → 需要语义图才 refresh → search_graph → trace_path/detect_changes → get_code_snippet 或读源码核验」的顺序、单一入口、缓存与 DACL 前置条件、worktree 语义（图不含 worktree 未提交新代码，新文件与新符号一律 `rg` 补充核验）、`list_projects`→`index_status`→`check_index_coverage` 的 CLI 等价写法与降级/证据条款；`scripts/lib/install-planner.js` 的 `toolDiscoveryLine` 与 `codebaseMemoryMcpLine` 改为带可判定触发信号的表述（跨文件符号、调用链、影响面、架构 → 先跑 `codebase-memory status`；单文件文本、配置、日志 → `rg`；AST 结构 → `ast-grep`），`docs/hooks.md`、`AGENTS.md` 与 `adapters/install-map.json`（3 个新增模块条目 + retired 登记）同步。验证：`pnpm check`（lint 250 文件 0 error、typecheck、validate、tests-catalog 1013 条、unit 149 通过、component 249 通过）、`pnpm docs:audit`（118 documents）、`pnpm runtime:audit`（warnings 为空）、`pnpm test:integration`（485 用例 / 484 通过 / 1 既有跳过 / 0 失败）、`pnpm test:matrix`（105 用例 / 104 通过 / 1 既有跳过 / 0 失败）、`pnpm smoke:lifecycle`（full-dry-run / full-write / full-validate / full-doctor 全 exit 0）、`pnpm eval:check`、`git diff --check`；真实验收用 0.11.0 二进制跑通端到端 provisioning（`VIBE_HARNESS_REAL_TOOL_INTEGRATION=1`，124 秒通过，跑后已清理探测缓存），MCP `tools/list` 断言 17 个工具，并用已安装 Hook 实跑断言 `trace_path`、`index_repository` 放行而 `delete_project` 仍拒绝，另为 Unicode 根路径别名替换、worktree 映射与 DACL 失败诊断各补断言；新增组件用例锁定显式工具分类表，新增集成用例覆盖 0.11.0 信封别名、freshness 端到端（fixture runtime）、doctor 报告缓存位置与陈旧副本、uninstall 清理外移缓存（dry-run 与 `--write`），新增 matrix 用例覆盖缓存前检的两条分支（DACL 拒绝降级 / 非 DACL 失败不降级）。指纹漂移为 aggregate + rules 两组，按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --confirm-reference-update --force` → `eval:sync --write` → `eval:replay --write` 再生成。BL-CNAS 的真实验收（升级 provision、`status` 期望 `stale` → `refresh --write` → 复测 `fresh`、用 `frontend/apps/business-web/*` 与 `AuditPersonRefInput` 探针确认图已更新、确认迁移后只剩一个缓存目录）属独立授权步骤，本批次未执行。
- 项目专属规则渲染与 browser-smoke 探针修复：`docs/rules/project-specific-rules.md` 是随 pack 安装并按目标项目渲染的规则模板，其余画像行都取 `{{projectProfile.*}}` 占位符，只有「包管理器」硬编码 `pnpm`，npm、yarn 或 Maven 目标项目因此拿到与自身事实不符的条目；该行改为 `{{projectProfile.packageManager}}`，与同文件其余占位符同源渲染。`scripts/lib/tool-provisioning/subprocess.js` 的 `browser-smoke` 阶段此前以 `probeTool: true` 调用 `runMcpHandshake`，而工具发现按 `tool.name === probeTool` 做字符串比对，布尔值恒不等于任何工具名，该阶段对任何 MCP server 都以 `MCP_TOOL_MISSING` 结束（`tools/list` 未返回数组时退化为握手超时），Chrome 启动失败因此无法观测；现改为探针 `list_pages` 并把 JSDoc 的 `probeTool` 类型从 `boolean` 改为 `string`，探针失败仍经 `tools/call` 的 JSON-RPC error 或 `isError` 映射为 `CHROME_LAUNCH_FAILED`。既有用例直接以字符串 `probeTool` 驱动 `runMcpHandshake`，`defaultPhaseRunner` 的 browser-smoke 分支本轮未新增回归用例，缺口随交付说明报告。验证：`pnpm check`（unit + component 全绿）、`pnpm eval:check`、`node --test tests/integration/project-profile.test.js`（21 通过）、`node --test tests/matrix/tool-provisioning.test.js`（46 通过 / 1 既有跳过 / 0 失败）、以 `packageManager: npm@10.8.0` 的目标项目 `init` 加 `install --dry-run` 实测渲染出目标项目自身的包管理器、`git diff --check`；rules 组资产指纹按设计漂移，经 `eval reference`、`eval:sync` 与 `eval:replay` 正规入口再生成，`eval:check` 通过。
- 分层验证与「最小充分成本」执行合同收口：新增常驻规则「成本与升级」（`docs/rules/governance-core.md`），把默认 Fast Path（必要事实 → 最小计划 → 直接实施 → 局部快速验证 → 简洁交付）与九类高成本动作（全仓扫描、完整 Spec/Plan/Tasks、多 Agent、Worktree、全量测试/E2E、全量 Build/Lint/Typecheck、深度 Review/Red Team、全量文档更新、长时 Benchmark）的触发条件写成同一条合同——触发条件是必要而非充分，升级须记录原因与代价，且成本判定与授权红线正交；升级阶梯为 Progressive Context Loading → Changed-Scope Validation → Risk-Based Review → Async Deep Verification → Final Gate，可按需跳过。**行为变化**：`vibe-harness verify --project <path>` 在不传 `--tier` 时默认只跑快速层（quick），交付收据的 `scopeStatus` 为 `partial`、退出码仍为 0，但不代表集成或发布完成；此前的「旧四项」语义改由 `--tier deep` 或 `--tier all` 表达，`--full` 仍是「完整风险矩阵 + 全层」的单一入口且与 `--tier` 互斥。默认调用时若快速层为空则回退到最便宜的非空层并记录 `tierFallback`，显式 `--tier` 不回退（空选择保持 `PROJECT_VERIFICATION_NO_CHECKS` blocked）。收据新增 `executionTier`、`tierSource`、`scopeStatus`、`deferredChecks`、`nextTier` 与 `tierFallback`（`recovery.hint` 改为按 `nextTier` 生成）；`vibe-harness verify:focused` 新增 `--tier quick|standard|deep|all`，`--run` 默认只执行快速层并把未选检查打印为 deferred。投影与文档同步：`AGENTS.md` 受管块与手工节、四个 adapter 模板、`docs/rules/test-rules.md`、`docs/rules/ai-collab-rules.md`、`docs/rules/role-routing.md`、`docs/rules/project-specific-rules.md`、`docs/templates/workflow.md`、`README.md`、`README.en.md`、`docs/migration-guide.md` 与 `CONTRIBUTING.md` 命令边界改为「默认快速层、升级需显式」并只加指针不复制正文。不新增离线或在线 Eval 用例、不加 Hook/CI 硬门禁；本轮不做全量文档刷新，`docs/catalog.json` 无需新增条目；`vibe-harness.config.json` 新增 `validationCommands.tiers` 声明本仓库三层（quick 为 `pnpm lint`、`pnpm typecheck`、`pnpm test:unit`、`pnpm test:component`，standard 为 `pnpm test:integration`，deep 为 `pnpm eval:replay`、`pnpm test:e2e`、`pnpm test:matrix`、`pnpm smoke:lifecycle`），`schemas/project-config.schema.json` 与 `docs/schemas/` 镜像新增可选 `validationCommands.tiers` 结构（quick/standard/deep 均为元素非空且去重的字符串数组）。验证：`pnpm check`、四个受影响的集成文件（`install-dry-run`、`project-profile`、`project-verification`、`verify-focused`，66 用例全部通过；其中 `verify-focused` 的挂起回收用例与 `pnpm check` 的文档 catalog 用例各出现一次 Windows 清理/并行竞态抖动——`EBUSY rmdir` 与 `tmp-docs-*` fixture 在扫描中被并行清理——原样复跑即通过，两处均不在本批次改动面内）、`pnpm eval:check`、`git diff --check`；`project-profile.test.js` 的过时断言（三层命令括号位置、`--tier standard|deep|all` 升级句）与 `package.json` 重复 `test:unit` 键在本轮修正后随上述检查验证。rules 组资产指纹按设计漂移，按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --confirm-reference-update --force` → `eval:sync --write` → `eval:replay --write` 再生成。
- 角色投影全面优化：描述真值层英文化、8 宿主按已验证契约投影与 doctor 四级状态（审查 P1–P6）：`manifests/roles.json` 的 7 个内置角色 `description`/`routing.when`/`routing.avoid` 全量英文化；投影层新增 `projectRoleDescription()` 作为唯一组合规则（`routing.mode: explicit` 的角色前置 `[Explicit invocation only; do not auto-select. `，组合串在函数内 fail-closed 校验单行、纯 ASCII 与 `MAX_PROJECTED_DESCRIPTION_LENGTH = 300`），所有宿主原生 frontmatter 与 Codex TOML 的 description、以及 `.agents/roles/index.md` 的 description/适用/避免三行全部改取组合串，索引与原生文件同源同语言。各宿主只投影已核实键位：OpenCode（唯一 native 权限宿主）补 `task`/`webfetch`/`websearch` 恒 deny、可执行预设 `external_directory: ask` 与非可执行预设 deny，并把 bash 由单条 `"*"` 改为 catch-all 在前、184 条无参数只读命令 allow 在后的前缀映射（前缀由 `runtime/hooks/lib/read-only-commands.mjs` 新增导出 `readOnlyCommandPrefixes()` 从既有分类表派生）——执行计划原文写的是把 `"*"` 放最后一条，与官方文档「last matching rule winning，catch-all 放最前」相反，落地按官方语义修正，否则白名单会被 catch-all 覆盖；Claude 按本次安装实际解析到的 server 枚举 `mcp__<server>`（无 server 时不写任何 `mcp__` 键）、新增 `skills:` 预加载且不把 `Skill` 写进 `tools`、analysis 预设加 `permissionMode: plan`；Gemini 只在安装含 MCP server 时追加 `mcp_*`；Qoder 改为只写宿主可解析的 `name`/`description`/`skills`/`mcpServers`，移除无效的 `tools` 键；ZCode 增 `skills` 与 analysis/security-review 的 `permissionMode: plan`；Antigravity 工具名按二进制核实改为 `view_file`/`grep_search`/`list_dir`/`replace_file_content`/`write_to_file`/`run_command` 并固定为 `configured-unverified`；Codex 不新增键位，Skill 与 MCP 由父会话继承（官方契约，属预期）。`resolveRoleInstallEntries()` 新增可选 `resolvedCapabilities` 入参（缺省不注入任何能力键位，向后兼容），安装计划器两处调用点传入本次解析结果，函数内对未知宿主、非 `vibe-harness-*` server 名与非法 Skill 名 fail-closed。`manifests/adapters.json` 每个 adapter 新增 `roleProjection.toolBinding`（native / prompt-guarded / configured-unverified）并成为 schema 必填；doctor 的角色报告拆成 `fileGenerated`/`hostActivated`/`toolBinding`/`currentTaskExecutable` 四级、合并 `status` 只描述第一级，新增 `ROLE_TOOL_BINDING_UNVERIFIED` 告警。`scripts/lib/roles-audit.js` 新增投影 description 逐字/单行/ASCII/≤300 与 explicit 前缀断言、role `name` 必须等于 Prompt H1（拦截 P3 类漂移）、OpenCode bash 顺序与命令来源断言，以及逐宿主能力键位断言（Qoder 不得出现 `tools`、Claude 必须逐字枚举已安装 server、不可绑定宿主不得出现 mcp/skills、Antigravity 工具名白名单），期望表独立于投影助手维护。`roles/prompts/technical-release-manager.md` 的 H1 修为「发布就绪审查者」；`docs/rules/role-routing.md` 新增「角色描述契约」，`docs/roles.md` 新增「角色描述」与 8 宿主映射表并改写 doctor 口径；`tests/role-projection.test.js` 增至 17 个用例（描述契约、能力绑定、fail-closed、OpenCode 权限面、审计负向），`tests/opencode-adapter.test.js` 新增落盘后 agent 文件的端到端断言。真机：Codex 0.147.0 上派生 `agent_type: chief-architect` 与 `senior-engineer` 两个子 Agent，两者都拿到本仓库角色契约正文（角色文件确实被宿主加载），但注入的权限说明都是父会话 sandbox（本机为 danger-full-access 与 CLI 的 read-only），说明角色级 `sandbox_mode` 在这条派生路径上未独立生效——已作为新证据补入 TD-2026-09-15-2，Antigravity 与其余宿主真机仍是缺口，`currentTaskExecutable` 保持 false。验证：`pnpm check`（381 项）、`pnpm roles:audit`（7 roles、0 errors、0 warnings）、`pnpm skills:audit`（15 Skills、0 findings）、`pnpm docs:audit`（115 documents）、`pnpm smoke:lifecycle`、`git diff --check`，并重放一次自安装（`--write --confirm-red-zone --force`，written 138 / retired 0）。`pnpm test:integration` 在加入本批次用例前一次全绿（320 通过 / 1 既有跳过 / 0 失败）；加入用例后两次运行都被同机其它项目构建的负载打成超时（一次是 TD-2026-09-15-8 的墙钟断言，独立测量 5329 / 9983 / 7937 ms；一次是 `tests/cross-platform-adapters.test.js` 的 120 秒单测上限，该文件单独运行 42/42 通过但耗时 107.8 秒），两者都不是断言失败且不在本批次改动面内，同一份文件清单把单测预算放宽到 600000 ms 后为 321 通过 / 1 既有跳过 / 0 失败；两条负载证据已补进 TD-2026-09-15-8，本批次未改该断言或超时预算。指纹漂移为 config、hooks、rules 三组加 aggregate（比计划预估的「config 与 rules 两组」多出 hooks 组，归因 `runtime/hooks/lib/read-only-commands.mjs` 的新导出），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --confirm-reference-update --force` → `eval:sync --write` → `eval:replay --write` 再生成，`pnpm eval:check` 与 `pnpm eval:replay` 通过。
- 治理规范与工作流审计落盘并建立 inventory 快照治理（审查批次 W4）：新增 `docs/inventory/governance-audit-2026-09.md`，把对 21 条规则、7 角色、12 原生 + 3 集成 Skills、scripts/runtime/.agents 三层工具链、manifests/schemas 与 eval 体系的系统性审查固化为 31 项分级台账——P0 6 项（A-01–A-06，投影漂移与 typecheck 口径）、P1 12 项（B-01–B-12，规范口径与工作流补强）、P2 路线图 10 项（C-01–C-10，只记录不执行）、接受项 3 项（E-01–E-03），每项带 file:line 证据与处置提交（W1=`d4bb676`、W2=`ec2c2b1`、W3=`aaf650a`），并附业界最佳实践对照（AGENTS.md 安装面即真值、Agent Skills description 是唯一路由真值、生成物靠再生成防漂移、测试发现优于手工枚举、docs-as-code 快照治理）与 2026-09-15 快照基准（Skills 12 原生 + 3 集成、规则 21、manifests 8、schemas 27、docs 镜像 16、`pnpm check` 376 项、`pnpm test:integration` 320 通过 / 1 既有跳过、`pnpm eval:check` 通过，每项附再生成方式）。`docs/README.md` 审计参考节新增 inventory 快照规则：含计数口径的条目必须标注快照日期与再生成方式，计数被取代的快照移入 `archive/`、原文不回溯改写；据此为 `ai-eval-investigation.md`（2026-07-30 快照）与 `harness-superpowers-comparison.md`（2026-09-05 快照）补快照口径标注。新文档经 `pnpm docs:sync --write` 注册进 `docs/catalog.json` 与文档索引。验证：`pnpm docs:audit`（115 documents）、`pnpm skills:audit`（15 Skills、0 findings）、`pnpm check`；docs-only 变更未触碰 eval 资产指纹。
- 测试枚举守护、forbiddenProjectTerms 外移与 worktree 双入口契约（审查批次 W3，P1 B-08–B-12）：新增 `scripts/lib/test-enumeration.js` 并接入 `pnpm lint`，把盘上 `tests/*.test.js` 与 package.json 三个测试清单比对，未登记即失败（保留显式清单的可控性，不改为 glob 执行），`tests/test-enumeration.test.js` 锁定三类漂移。`scripts/lib/project-config.js` 硬编码的 4 个先前项目词外移为可选配置字段 `forbiddenProjectTerms`（schema 校验唯一非空字符串数组），词表移入本仓库 `vibe-harness.config.json`，pack 通用核心目录归零；`tests/redaction.test.js` 改为双扫描——pack 目录全量扫描 13 个源项目词、config 只扫未声明词，并把声明集钉死防止例外静默扩大；顺带修复脱敏扫描 includeDirs 的死条目（根级 `rules/` 不存在、实际规则在 `docs/rules/`，此前被静默跳过）。worktree 双入口分工文档化：`runtime/commands/run.mjs` 的 `--help` 从任意命令位路由到 help 回执并携带 worktree 子命令契约、未知子命令错误列出全部合法值，开发面 `scripts/worktree.js --help` 与 `docs/rules/git-rules.md`、`CONTRIBUTING.md` 互指分工；补 `docs/schemas/project-config.schema.json` 镜像并纳入 governedAssets、catalog 与 docs/README 索引。验证：`pnpm check`（376 项）、`pnpm test:integration`（320 通过 / 1 既有跳过）、`pnpm skills:audit`；config 与 rules 两组资产指纹按设计漂移（hooks、skills 未动），归因 config.hash=config+schema 变更、rules.hash=git-rules.md 后，按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --force --confirm-reference-update` → `eval:sync --write` → `eval:replay --write` 再生成，`pnpm eval:check` 通过。
- 规范口径统一与 skill description 标点收敛（审查批次 W2，P1 B-01–B-07）：`README.en.md` 的 memory 模块措辞对齐中文版并补 Profiles 角色说明；`docs/rules/role-routing.md` 内嵌的 Skill 选择合同改为指向 `docs/rules/agent-skill-routing.md` 的单源表述；`docs/rules/ai-collab-rules.md` 在 Linear 状态映射表补 `linear-workflow.md` 回指；`docs/templates/task.md` 与 `templates/task.en-US.md` 的 DAG result 语义复述改为指针加字段名清单；`docs/audits.md` 写明 `--kind all` 聚合 memory、review、improvements 而不含 cleanup 的语义；`docs/evals.md` 显式声明 onlineRunner 为 null 与单一 offline reference 的接受状态；clarify-requirements 与 frontend-design 的 description 标点统一为 em-dash（SKILL.md、`.agents` 投影与 manifests 同步），`tests/eval-runner.test.js` 的知识覆盖用例同步移除 browser-verification（W1 移除投影后 test:eval 滞后）。验证：`pnpm check`（含 docs:audit）、`pnpm test:unit`、`pnpm skills:audit`；config.hash（W1 config 变更滞后）、rules.hash（role-routing/ai-collab）、skills.hash（description）与 skills.fileCount 121→128（W1 metadata +9、投影清理 -2）四组指纹归因核对后，按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --from <run> --write --force --confirm-reference-update` → `eval:sync --write` → `eval:replay --write` 再生成，`pnpm eval:check` 通过。
- 自安装投影收敛与 typecheck 验证口径单源化（审查批次 W1，P0 A-01–A-06）：`adapters/install-map.json` 补 `runtime/evals/lib/knowledge-coverage.mjs` 条目并补齐 12 个原生 Skill 的 metadata.json 映射（此前 2/12），`.agents` 投影重新自安装至与 pack 源一致，消除未注册资产的静默漂移；`vibe-harness.config.json` 纳入 project-scripts 模块并显式接入 `pnpm typecheck`，渲染端 `resolveValidationCommands` 改为 config 单源（执行端 null 即 skip 的契约不变），否决执行侧检测回填以保住「项目配置完全决定验证命令」契约，AGENTS.md 手工节 typecheck 口径同步为「已并入 `pnpm check`」，测试改为断言新契约并以 doesNotMatch 守护旧口径回归；移除无 install-map 与 install-state 依据的未管理投影 `.agents/skills/browser-verification/` 与 `.agents/runtime/lib/rtk-environment.mjs`（pack 源保留，由 skills:audit 记账）；`runtime/evals/observers.json` 经消费面核查（manifests/capabilities.json 与 scripts/eval-check.js）保持仅存在于 pack 源；self-install-check 注释中构成假路径的大写 `AGENT_SKILL_ROUTING.md` 表述改写（消除 CLEANUP_REFERENCE_MISSING），三个测试文件的 `--verbose` 干跑 stdout 改用 8MB maxBuffer 适配计划增长。验证：`pnpm test:integration`、`pnpm smoke:lifecycle`、自安装一致性校验、`pnpm check`；config 组指纹按设计漂移，滞后至 W2 批次随 rules 与 skills 组一并归因重生成。
- Hook 文档口径统一、doctor/安装新增重新信任提示并补实施台账（审查 H-03/H-05/H-11）：`runtime/hooks/README.md` 删除「Missing Git roots or runtimes fail with a non-zero exit」，改写为「宿主 deny 契约」，并把与 `manifests/adapters.json` 矛盾的 `Stop is unsupported on every host` 修正为「Codex 与 Claude Code 为 not-projected、其余宿主为 unsupported」，同时补 PATH 上没有 `node` 或 Node 版本过低时的残余风险；`docs/hooks.md` 同步失败契约（宿主只把 `exit 2` 或 deny 决策当作阻断，其余非零退出会被记为 Hook 运行失败并继续执行工具调用）、PreToolUse `matcher` 为 `.*` 的触发面、本地宿主函数工具的显式名单与语义变化，并写明 5 秒内部预算与 + 8 秒引导预算。`scripts/lib/runtime-diagnostics.js` 新增警告码 `HOOK_TRUST_REREVIEW_REQUIRED`：宿主记录为 `trusted-enabled`、而已安装 `.codex/hooks.json` 当前哈希与 install-state 记录的 `targetHash` 不一致时（定义在安装后被改写，或由更新的 pack 渲染）追加警告，文案用「可能要求重新信任」而不作因果断言——宿主的信任哈希算法不可从项目复现；`runtimeHookWarnings` 增加可选第二参数 `{ definitionChanged }`，`scripts/vibe-harness.js` 在 install／validate／doctor 三处接入，install 另外比较本次安装前后定义文件的哈希，因此「本次安装改写了定义、install-state 已同步」也能报出；新增导出 `hookDefinitionDrift()` 与 `hookDefinitionPath()`。测试：`tests/runtime-diagnostics.test.js` 新增用例覆盖匹配、无记录、记录指向他目标、缺定义、改写后漂移，以及「只有 `trusted-enabled` 才提示、未声明变更时不提示」。台账：`audit-reports/2026-09-15-agent-config-review.md` 新增第 16 节记录 H-01–H-11、三个批次、契约与接口变化、验证证据与未闭合项，附录 B 补两次提交 SHA。验证：`node --test tests/runtime-diagnostics.test.js`（7/7）、`pnpm lint`、`pnpm typecheck` 通过；本仓库自安装面重放（`.agents/runtime/hooks/README.md`）。
- Hook 失败路径由「非零退出」改为 fail-closed 并单源化引导命令（审查 H-01/H-04/H-06/H-07/H-08/H-10）：新增 `scripts/lib/hook-bootstrap.cjs` 作为引导脚本唯一真源（CJS 单行，可独立审阅与调试），`scripts/lib/template-renderer.js` 改为读入该文件文本注入 `hookBootstrapCommand`，删除手写双重转义字符串；注入前校验载荷字符白名单，出现双引号、反斜杠、反引号、`$`、`%` 或换行即抛错，避免 Windows cmd/PowerShell 与 sh 的引号与变量展开差异。根定位改为向上查找 `.git`（目录或文件，兼容 worktree/submodule），不再调用 `git rev-parse`，也不再依赖 PATH 上的 git。失败分支（找不到根、缺 `.agents/runtime/hooks/codex-hook.mjs`、子进程启动失败、超过 8 秒预算）统一输出宿主可识别的 deny 载荷并 `exit 0`，理由以 `[VIBE_HARNESS_HOOK:BOOTSTRAP_UNAVAILABLE]` 开头并同时写一行 stderr：宿主只把 `exit 2` 或 deny 契约当作阻断，此前非零退出会被记为「Hook 运行失败」并继续执行工具调用。deny 形状按宿主分形（codex/claude/qoder/zcode 的 PreToolUse 走 `hookSpecificOutput.permissionDecision`、PermissionRequest 走 `hookSpecificOutput.decision.behavior`，cursor 走 `{continue:false,stopReason}`，antigravity 走 `{decision:"deny",reason}`，完全未知宿主保留 `exit 2`）。引导进程只向受管运行时透传 PATH、PATHEXT、SystemRoot、SystemDrive、TEMP、TMP、HOME、USERPROFILE、CODEX_HOME 与两个 `VIBE_HARNESS_EXECUTION_ENVELOPE*` 开关，再追加 `VIBE_HARNESS_GIT_ROOT`，不再整份继承 `process.env`。`runtime/hooks/codex-hook.mjs` 增加 5 秒内部 watchdog（`VIBE_HARNESS_HOOK_BUDGET_MS` 只能缩短、不能延长），超预算写出与常路径同形的 deny JSON 后退出，并由单一写入者保证 stdout 只有一份决策；`runtime/hooks/lib/execution-envelope.mjs` 的 `gitOutput` 与 `isAncestor` 补 3 秒超时，与 `lib/context.mjs` 对齐。`adapters/codex/hooks.template.json` 删除与 `command` 逐字相同的 `commandWindows` 覆盖字段。测试：`tests/hook-bootstrap.test.js` 重写为 7 个用例（根/多级目录/worktree 解析、无根与缺受管运行时 → deny JSON + `exit 0`、逐宿主与事件的 deny 形状与 `createHostHookResult` 一致、未知宿主 `exit 2`、载荷单行且无禁用字符、环境白名单不泄漏 `OPENAI_API_KEY`/`AWS_SECRET_ACCESS_KEY`），`tests/hook-runtime.test.js` 新增「超预算返回 fail-closed 决策」用例，`tests/codex-adapter.test.js` 断言投影内嵌单一引导源且无重复 Windows 覆盖，`tests/hook-installation.test.js` 的命令不变量改为命中 `.git` 与 `process.execPath`（不再匹配 `rev-parse`）并新增安装产物在无 Git 目录下按事件返回 deny 的端到端断言。验证：`pnpm lint`、`pnpm typecheck` 与 `node --test tests/hook-bootstrap.test.js tests/hook-runtime.test.js tests/codex-adapter.test.js`、`node --test tests/hook-installation.test.js` 全绿，`pnpm validate` 对本次钩子资产零漂移（同期工作区另有并行会话未提交的 `runtime/commands/run.mjs` 改动会使其报该文件的漂移，与本批次无关）；本仓库自安装面已按新模板重放；bootstrap 端到端耗时中位数 275.8 ms → 225.2 ms（12 轮交错实测，省去一次 git 子进程），仅作记录不作门禁。
- PreToolUse 覆盖所有本地函数工具并补全只读判定（审查 H-02）：`adapters/codex/hooks.template.json` 的 PreToolUse matcher 由工具名枚举 `Bash|Edit|Write|ApplyPatch|apply_patch|mcp__.*` 改为 `.*`，与 claude/cursor/qoder/zcode/antigravity 模板一致，使新出现的宿主工具不再静默绕过安全策略，也不依赖 `*` 在宿主端的特殊语义。`runtime/hooks/lib/read-only-commands.mjs` 的只读工具集合新增 `view_image`、`update_plan`、`agent`、`spawn_agent`、`task`、`read_file`、`read_thread`、`read_thread_terminal`、`list_threads`、`list_archived_threads`、`wait_threads`、`list_agents`、`list_projects`、`get_goal`、`get_handoff_status`；`write_stdin`、`create_thread`、`fork_thread`、`automation_update`、`handoff_thread`、`set_thread_*`、`send_message_to_thread`、`followup_task` 与一切未列名工具仍走 Execution Envelope（无 Envelope 即拒）。语义变化：`update_plan`／`view_image`／`Agent` 由「不判定」变为「放行」，`write_stdin`／`create_thread`／`automation_update` 由「不判定」变为「拒绝」，是本轮唯一的行为收紧点。测试：`tests/hook-read-only-classification.test.js` 新增两组表驱动用例（15 个放行工具断言 `readOnly`、无 effect、无高风险理由、策略放行与端到端 `{}`；8 个写入类工具断言端到端 deny 且理由含 `EXECUTION_ENVELOPE_MISSING`），`tests/codex-adapter.test.js` 断言渲染后的 PreToolUse matcher 为 `.*`。验证：`node --test tests/hook-read-only-classification.test.js`（14/14）、`node --test tests/codex-adapter.test.js`（4/4）、`pnpm lint`、`pnpm typecheck` 通过；本仓库自安装面已重放（`.codex/hooks.json` 的 matcher 与 `.agents/runtime/hooks/lib/read-only-commands.mjs`）。
- 新增项目级安装预设，把「full + 全部稳定插件 + Linear + memory + 多宿主」压缩成一次声明：`manifests/install-presets.json` 与 `schemas/install-preset.schema.json` 声明预设目录（当前只有 `everything`：`profile=full`、`addModules=["memory"]`、`plugins={all:true,add:["linear"]}`、`allowPreview=true`、`provision=true`），`scripts/lib/install-preset.js` 做只读校验（未知 profile、未知模块、未知插件、重复 id、linear 与 linear-readonly 同时出现、以及缺少「覆盖全部 stable 插件」的声明都报错）并提供唯一解析入口 `resolveInstallSurface()`，`scripts/lib/pack-validation.js` 与 `pnpm check` 接入该校验。CLI 新增 `init --preset <id>`、`init --targets codex,zcode,opencode`（与 `--target` 互斥，重复项、未知宿主、空列表报错，解析保持顺序）与 `install --preset <id>`；其余命令不接受 `--preset`，只从配置读取。`vibe-harness.config.json` 新增可选 `preset` 字段（schema 枚举固定为 `everything`），preset 存在时 profile 必须等于其展开结果且不得同时声明 `plugins` 或 `modules`；CLI 显式 `--profile`／`--modules`／`--plugin` 与已声明 preset 冲突时报错，避免配置与 install-state 静默分叉。`install --preset` 在配置缺少该 preset 时于同一安装事务内补齐：配置属红区，未带 `--confirm-red-zone` 时直接拒绝，dry-run 只在 `configUpdate` 中报告将写入的内容。install／validate／doctor 的 JSON 新增 `preset`（null 或 id），summary 新增 `preset: <id>` 行，`provisioning` 新增 `source: cli|preset`；`full`、`--plugin all`、`resolveModuleSelection()` 与 `parsePluginsOption()` 的语义不变（`all` 仍只展开 6 个稳定工具、不含 Linear，Linear 只能显式选择或由预设显式带出），install-state 仍记录展开后的 `requestedPlugins` 与 `requestedModules`，validate／doctor 因此天然重放同一安装面。多宿主沿用既有链路：`init --targets` 写入配置的 targets 数组，不带 `--target` 的 install／validate／doctor／diff 处理全部目标。文档：`README.md`／`README.en.md` 在「快速开始」之前新增顶层「全量安装（推荐）」章节（短提示词、等价命令与配置 JSON 示例，中英命令块与 JSON 逐字一致），`docs/specs/vibe-harness-tooling-modules-spec.md` 新增「安装预设」小节写明 preset 与 `--plugin all` 的边界，`docs/migration-guide.md` 补已有安装启用预设的步骤。测试：新增 `tests/install-preset.test.js`（7 个用例，已纳入 `test:unit`）与 `tests/target-validation.test.js` 的 4 个用例（`--targets` 逗号解析与拒绝、preset dry-run 计划、三宿主真实写入后 validate／doctor 重放、`install --preset` 持久化与红区门禁），`tests/mvp-spec.test.js` 新增「只有 preset 才能让 full 安装带上 linear 与 memory」并保留既有 full 不含外部工具与 memory 的断言。验证：`pnpm check` 329/329、`pnpm test:integration` 320 pass／0 fail／1 既有跳过、`pnpm smoke:lifecycle` 10 步全绿、`pnpm eval:check`、`pnpm eval:replay`、`pnpm validate`、`pnpm lint` 与 `git diff --check` 通过。`manifests/` 与 `schemas/` 的变更使 eval config 组指纹按设计漂移（hooks、rules、skills 三组未动：config fileCount 34→36、config hash 747aed3e…→9f607cc3…、aggregateHash 0e6b4a23…→811221f0…），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --write --force --confirm-reference-update` → `eval:sync --write` → `eval:replay --write` 再生成 reference、`.agents/evals/` 镜像与 replay 产物，旧文件备份于 `.vibe-harness/backups/2026-09-15T08-20-28-807Z/` 与 `.vibe-harness/backups/2026-09-15T08-20-45-969Z/`。
- 新增过期资产清理能力，把「识别」做成确定性扫描、把「删除」留给人逐项确认：`skills/core/stale-cleanup/` 新增 core 原生 Skill（`$stale-cleanup`，允许宿主隐式选择，默认只读；`references/asset-classes.md` 承载六类资产的判定细则、误报边界与恢复方式，`agents/openai.yaml` 与 `metadata.json` 只管展示与触发），并新增 `vibe-harness audit --project <path> --kind cleanup`（`scripts/lib/cleanup-audit.js` 接入 `scripts/lib/project-audit.js` 的 kind 选择与 `schemas/audit-report.schema.json` 的 `cleanup` 收据，`--write` 一律拒绝，`--kind all` 仍只聚合 memory、review、improvements）。扫描面由 `git ls-files --cached --others --exclude-standard` 枚举，因此 `.gitignore`、`node_modules`、`.git` 与生成目录不进扫描；证据分两档——确定性项 `CLEANUP_REFERENCE_MISSING`(error)、`CLEANUP_INDEX_STALE`/`CLEANUP_ORPHAN_ASSET`/`CLEANUP_DOC_ORPHAN`/`CLEANUP_DOC_UNCATALOGED`(warning)，启发式候选恒为 `info`（`CLEANUP_UNREFERENCED_FILE`、`CLEANUP_UNUSED_EXPORT`、`CLEANUP_DOC_STALE`），未覆盖的判定写入 `details.cleanup.skipped`。索引新鲜度按 `.codebase-memory/artifact.json` 记录的 commit 与时间确定性核对（构建 commit 与 HEAD 不一致、索引超过半年都报 `CLEANUP_INDEX_STALE`），产物缺失或不可读时报 `CLEANUP_INDEX_UNAVAILABLE`(warning)，不伪造结论。误报边界写进实现：markdown 链接按引用文件所在目录解析并以仓库根写法作备选读法，围栏代码块内是示例与声明，`evals/references/` 由 `eval reference --write` 生成，测试与测试夹具、`.agents/`、`audit-reports/`、`docs/archive/`、`docs/inventory/`、`CHANGELOG.md` 不作引用来源，非工程前缀与 `${}` 占位符不参与判定；明确不做 manifest→source 校验（该职责属 `pnpm validate`）。预算按确认调整：单个原生 Skill 入口 50→150 行、原生总量 300→450 行、原生身份预算 1500→1750 字符（`scripts/lib/pack-validation.js` 与 `tests/skill-closure.test.js` 同步，理由写在注释里）。注册与文案：`manifests/skills.json`、`manifests/profiles.json`（core 八个→九个、full 十一→十二）、`adapters/install-map.json`（4 条 `skills-core` 条目）、`scripts/lib/install-planner.js`、`docs/rules/agent-skill-routing.md`、`docs/inventory/skills-optimization-zh.md`、`README.md`/`README.en.md`、`CONTRIBUTING.md` 与 docs/schemas 镜像。测试：新增 `tests/cleanup-audit.test.js`（缺失引用与候选分离、忽略目录与测试夹具、围栏示例与生成引用、catalog 证据映射、索引新鲜度三态、只读哈希与 `git status` 快照、`--write` 拒绝、schema 校验、`all` 不含 cleanup），并更新 `tests/project-audit.test.js`、`tests/skill-closure.test.js`、`tests/lint-discovery.test.js`、`tests/cross-platform-adapters.test.js`、`tests/manifest-schema.test.js`、`tests/mvp-spec.test.js`、`tests/rules-depth.test.js` 的清单与数量断言。验证：`pnpm check` 322/322、`pnpm skills:audit`、`pnpm docs:audit`（112 documents）、`pnpm eval:check`、`pnpm eval:replay`、`pnpm test:integration`（315 pass／0 fail／1 既有跳过）、`pnpm validate` 与 `git diff --check` 全绿；因 rules、skills、config 三组资产指纹按设计漂移（hooks 未动），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --write --confirm-reference-update --force` → `eval:sync --write` → `eval:replay --write` 再生成 reference、`.agents/evals/` 镜像与 replay 产物（旧 reference 备份于 `.vibe-harness/backups/2026-09-15T07-05-41-460Z/`，skills 组 113→121 文件）。对本仓库执行 `audit --kind cleanup` 得到 2 条已确认项（代码索引停留在 f561d4a；`docs/rules/AGENT_SKILL_ROUTING.md` 在大小写敏感文件系统上不存在）与 54 条候选线索，均为仓库既有状况、本轮未删除任何文件。
- 规则索引只列实际安装的规则（修复下面一条记账里 `pnpm test:unit` 那一处红）：`scripts/lib/rules-index.js` 新增 `installedRuleIndex()`，用当前计划的安装目标过滤 `manifests/rules.json` 的全量目录；`scripts/lib/install-planner.js` 的已安装表面改为渲染过滤后的索引。此前最小、core 与 full profile 都会把未安装的可选规则（`codebase-memory-mcp`、`chrome-devtools-mcp`、`linear-workflow`、`rtk`、`ast-grep`）写进宿主常驻指令，宿主会按索引路由到项目里不存在的文件并高估项目能力，`tests/mvp-spec.test.js` 的 `rendered AGENTS surface matches minimal, core, and full profile installs` 因此在 HEAD 上恒红。同一处把已安装表面的 targets 从「本次计划的 write 动作」换成「计划执行后仍在项目中的目标」（排除 `discard` 与 retire 动作）：kept 文件会随 `--force` 与本地内容在 write／user-modified／conflict 之间改变分类，用 write 集合推导会让常驻索引随无关的本地编辑抖动。新增 `tests/rules-index.test.js` 用例锁定「只列计划安装的规则」。本仓库自安装的 AGENTS.md 受管块按新渲染结果重放（索引从目录全量 21 条收敛为该 profile 实际安装的 15 条），`pnpm validate` 与 `tests/self-install-check.test.js` 的漂移报错同时消失。验证：`pnpm check` 304/304、`pnpm validate`、`pnpm eval:check`、`pnpm eval:replay`、`pnpm test:eval`（218 pass／0 fail／1 既有跳过）、`pnpm docs:audit`（112 documents）与 `git diff --check` 全绿；`pnpm test:integration` 除 `tests/project-verification.test.js:249` 的「超时必须 5 秒内返回」墙钟断言在高负载下取到 6.4 秒外全部通过（同一用例单文件复跑 17/17 通过，运行期间同机另有 60+ node 进程，本次改动不涉及该路径）。
- 测试规则按单一事实源与业界测试类型收敛（审查批次）：`docs/rules/test-rules.md` 的第一张表改名「行为风险类别」并显式声明路径到命令的唯一矩阵在 AGENTS.md（优先用 `pnpm verify:focused` 生成计划），UI 与数据行分别指向 frontend-rules 与 db-rules 的专项基线；测试类型表补回归、可访问性（WCAG 2.2 AA）、性能/负载、兼容性、韧性/故障注入五类，并把「封闭系统」改为「解析器、编解码与不受信输入边界」；验收状态统一到 governance-core 的 passed/failed/blocked/unverified（degraded 对应 blocked），覆盖率条款补增量覆盖率、关键模块变异测试与负控作为更强信号；工程约定新增测试数据（合成或最小化数据、生产 PII 与凭据不进 fixture）与确定性（固定时钟、随机种子、时区、locale）两条，flaky 隔离改为必须绑定可核对的技术债 ID、owner 与关闭条件，微小文案条款拆句并明确不覆盖目标项目自身 CI 门禁。此前随包安装到所有 profile 的 Vibe-Harness 私有实现细节改为一句「参考实现边界」，具体事实移入 `CONTRIBUTING.md` 新增的「测试实现（本仓库）」；与 governance-core、eval-driven-development、troubleshooting 重复的判定措辞收敛为指针。护栏：`scripts/lib/pack-validation.js` 的 test-rules 锚点由 22 条扩到 30 条（新增 verify:focused、分层运行、技术债 ID/owner/关闭条件、生产 PII、固定时钟/随机种子/时区/locale、WCAG 2.2 AA、负控等）；`docs/memory/TECH_DEBT.md` 新增 TD-2026-09-15-1 记账两处负载敏感 retry 标记（其注释原本引用的 TD-2026-09-01-2 不在清单中），`tests/tool-provisioning.test.js` 两处注释改指该条目。验证：`pnpm lint` 0 error、`pnpm typecheck`、`pnpm docs:audit`（112 documents）、`pnpm eval:check`、`pnpm eval:replay` 与 `git diff --check` 通过；`pnpm test:unit` 303/304，唯一失败是 `tests/self-install-check.test.js` 报的 AGENTS.md 规则索引漂移，来源是并行会话未提交的 rules-index/install-planner 索引过滤改动（本条只改 test-rules 内容，不改变已安装规则集合）。
- 前端规则补齐可判定门槛并消除与前端 Skill 的判据冲突：`docs/rules/frontend-rules.md` 从 47 行扩到 56 行，把此前循环定义的“对比度达标”“触摸目标遵循设计体系”换成可核对的验收口径——可访问性以 WCAG 2.2 AA 为基线（正文 4.5:1、大字 3:1、控件与状态 3:1），缩放与重排写死 200% 与 320 CSS px，焦点指示器对比度达标且不被固定头部、吸底栏与浮层遮挡，补齐模态与路由切换的焦点归属与归还、视图切换后的文档标题，指针目标不低于 24×24 CSS px（移动端无设计体系时取 44×44），拖拽或滑动类操作必须提供单击或键盘等价路径；表单底线补持久标签与 `autocomplete`/`name`、不得阻止粘贴与密码管理器或一次性验证码填充、同一流程不重复索取已提供信息、提交失败保留输入；输入安全由“不得直接注入 HTML”扩到属性、URL、CSS、SVG 四类下游与白名单净化，并要求 `postMessage` 接收侧校验来源与负载、发送侧限定目标来源，另加“前端不承担授权与密钥保管”；并发底线补幂等键、过期响应不得覆盖新状态、乐观更新回滚、缓存按身份隔离与有界重试；性能改为证据口径（优先现场 p75 或真实浏览器性能轨道，Core Web Vitals 作通用参照而非硬门禁），并补本地化、媒体、强制色、第三方脚本完整性与移动视口条目，同时保留“令牌体系或完整浏览器矩阵缺失不阻塞局部修复”的既有边界。同步修正前端 Skill 里与规则冲突的判据：`references/content-a11y.md` 与 `references/visual-craft.md` 不再写“优先 APCA 而非 WCAG 2”（WCAG 3 仍是 Working Draft 且对比度算法未定，验收基线为 WCAG 2.2 AA，APCA 只作补充复核），`references/performance.md` 的“超过 50 项列表虚拟化”改为按实测渲染成本决定，`references/interactions.md` 的命中目标依据区分 2.5.8（AA 24×24）与 2.5.5（AAA 44×44）；`skills/core/frontend-design/references/*` 与 `.agents/skills/frontend-design/references/*` 逐字同步。护栏：`scripts/lib/pack-validation.js` 的 frontend-rules 锚点由 7 条扩到 25 条（新增 WCAG 基线、320 CSS px、焦点归还、24×24、白名单净化、幂等键、现场 p75、可访问性树等），`tests/rules-depth.test.js` 的反漂移断言未触发。验证：`pnpm check` 303/303、`pnpm skills:audit`、`pnpm eval:check`、`pnpm eval:replay`、`pnpm test:eval`（218 pass / 0 fail / 1 既有跳过）与 `git diff --check` 全绿；因 rules 与 skills 两组资产指纹按设计漂移（config、hooks 未动），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --write --confirm-reference-update --force` → `eval:replay --write` → `eval:sync --write` 再生成 reference 与 `.agents/evals/` 镜像，旧文件备份于 `.vibe-harness/backups/2026-09-14T23-57-34-342Z/` 与 `2026-09-14T23-57-40-394Z/`。
- 评测驱动开发规则补齐判定与评测器有效性条款：`docs/rules/eval-driven-development.md` 由单段密集散文改为分节契约（评测类型 / 变更与判定 / 断言与 judge / 多轮与稳定性 / 运行面 / 产物与写入边界 / 套件治理 / 知识覆盖观察 / 与其他契约的关系），原有规范性条款逐条保留，另补齐四项此前缺失的可判定内容——capability 与 regression 分离且旧资产本来就能通过时记为 not-reproduced；负控（已知坏样本必须失败、好样本不得误判）与「judge 默认与被测模型分离、语义断言对齐人工标注前只能作报告信号」；判定规则显式引用项目配置阈值 `maxCapabilityRegression` 并要求改善超过抖动边界；运行面区分 contract-replay 只证明 suite/oracle/scoring 一致、Harness Evals 才是行为证据、`stub-behavioral` 当前无生产者。同时删除悬空引用 `AC-ID`（当前模板与 schema 已无该概念，仅存于归档任务与审计报告），给 flaky 补隔离并限期的退出路径，给完整 Trace 补 30 天留存上界，并补「确认的真实失败在同一变更内沉淀为 case」「保留不用于调参的 holdout」两条套件治理条款。Skill 双份同步（`skills/core/eval-driven-development/SKILL.md` 与 `.agents/skills/eval-driven-development/SKILL.md` 逐字一致），执行步骤补 RED/not-reproduced、负控、judge 分离、阈值与抖动边界、失败沉淀。护栏：`scripts/lib/pack-validation.js` 新增 `EVAL_CONTRACT_PARITY_TERMS` 与两条 `CONTENT_QUALITY_CHECKS` 条目——这份自述常驻契约的规则首次纳入 `pnpm validate` 锚点表，并与 Skill 共享同一组同步锚点，避免「修改须同步」只靠人读。验证：`pnpm check` 303/303、`pnpm validate`、`pnpm eval:check`、`pnpm eval:replay`、`pnpm test:eval`（218 pass / 0 fail / 1 既有跳过）与 `git diff --check` 全绿；因 rules 与 skills 两组资产指纹按设计漂移（config、hooks 未动），按 CONTRIBUTING 清单经 `eval run --write` → `eval reference --write --confirm-reference-update --force` → `eval:replay --write` → `eval:sync --write` 再生成 reference 与 `.agents/evals/` 镜像，旧文件备份于 `.vibe-harness/backups/2026-09-14T23-45-25-212Z/`，reference diff 仅含 `approvedAt` 与 aggregate/rules/skills 三个 hash 行。
- 关闭 TD-2026-09-14-3（签入的 offline replay 产物缺少正规生成入口）：`evals/results/vibe-harness-core.offline.json` 内嵌与 reference 相同的资产指纹并被 `pnpm eval:replay` 深比较，但 CONTRIBUTING「Eval reference 更新清单」与 `docs/evals.md` 此前只覆盖 reference 与 `.agents/evals/` 镜像，指纹漂移只能用 `buildOfflineRun()` 手工重放补齐、且没有旧文件备份（来源见下一条 reference 再生成的记账）。现在 `scripts/lib/eval-replay.js` 新增 `syncOfflineRunArtifact()`：读取现状、按 fingerprint 字段报告漂移、仅在 `--write` 时先把旧文件备份到 `.vibe-harness/backups/` 再写入确定性重放结果，文件缺失时直接创建而不产生备份；`scripts/eval-replay.js` 增加 `--write`／`--json`／`--help`，只读路径的输出与退出码保持不变，但失败从「replay differs from the checked-in result」升级为同时报出漂移的 fingerprint 字段与下一步。`--write` 先只校验 suite 契约（允许 run 与 reference 暂时不一致），写完后交叉比对 reference 指纹，仍不一致即以非零退出并给出再生成命令，避免只提升一侧。测试（`tests/eval-contract.test.js`）覆盖：漂移只报变化字段、`--write` 生成备份并写回确定性重放结果、重复运行报 `current` 且不再写入、缺失文件创建无备份、CLI 拒绝未知参数；对真实仓库执行 `node scripts/eval-replay.js --write --json` 得到 `status=current`、`reference=matched`，证明签入产物本身就是确定性重放输出。文档同步：CONTRIBUTING「Eval reference 更新清单」第 3 步与 `docs/evals.md` 增加该命令、备份位置与「先 reference、后 replay 产物」的顺序。
- eval offline reference 再生成（R-04 收尾；按 test-rules「reference 更新必须单独审查并显式确认」）：R-04 扩展 `schemas/eval-suite.schema.json`（`input.compaction`、`input.fixture.git`）使 config 组指纹按设计漂移；同一工作树另有 `docs/rules/agent-skill-routing.md` 的 kebab-case 改名与规则编号收敛及 `manifests/rules.json`、`manifests/capabilities.json` 的路径引用更新，rules 组一并漂移。归因用「HEAD 归档 vs 工作树」独立复核：HEAD 的 aggregateHash 2cb1164e…、config 76c74168…、rules 3f605a8d… 与旧 reference 逐位相等，hooks 与 skills 两组不变，说明漂移全部来自本分支未提交改动，没有结账形态或构建产物混入。再生成走正规入口 `vibe-harness eval run --project . --mode offline --write` → `eval reference --from <run> --write --force --confirm-reference-update`（旧文件备份于 `.vibe-harness/backups/2026-09-14T10-18-29-955Z/`），reference diff 仅含 approvedAt 与 3 个 hash 行；`evals/results/vibe-harness-core.offline.json` 由 `buildOfflineRun()` 确定性重放再生（同样只含 3 个 hash 行），`.agents/evals/references/` 镜像由 `pnpm eval:sync --write` 同步。再生成后 `eval run` 资产诊断为空、`reference.status` 为 matched，`pnpm eval:check`、`pnpm eval:replay`、`pnpm test:eval`（216 tests／215 pass／0 fail／1 既有跳过）与 `git diff --check` 全绿；replay 产物缺正规生成入口的缺口记入 TD-2026-09-14-3。
- 新增宿主受控真实压缩恢复用例（审查建议项 R-04／Finding F-05，关闭「checkpoint 真实压缩恢复证据薄」）：此前 execution suite 只能测「给出历史上下文后继续」，没有任何证据表明 Harness 能处理真实压缩——旧摘要覆盖实时状态、重复已完成步骤这类失败模式只能靠人工推断。现在 suite 支持 case 级 `input.compaction`（`resumePrompt`/`contextWindow`/`autoCompactTokenLimit` 必填，`activeObjective`/`terminalCondition`/`completedFacts`/`noRepeatSet`/`nextAction` 可选，schema `additionalProperties:false`），driver 先把 fixture 初始化为确定性 Git 仓库（`input.fixture.git`，固定 `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` 与 `core.autocrlf=false`，HEAD 可复算），再由新增的 `hostCheckpointEnvelope()` 用 `buildEnvelopeDraft()` 注入宿主 Execution Envelope v2：冻结真实 worktree/branch/HEAD、`allowedEffects:['workspaceWrite']` 并禁止其余 effect，checkpoint 刻意只记录过期的 `completedFacts` 与 `nextAction`，用于验证「旧摘要/旧 checkpoint 不得覆盖实时状态」。runner 侧新增 `hostCompactionContract()`（缺失或自相矛盾的声明 fail-closed）、`clientTokenEstimate()`、`compactionBudget()`、`compactionEvidence()`、`isHostExecutionEnvelope()`，并在缺 Envelope 时拒绝执行压缩 case。关键实测结论：宿主压缩触发阈值比对的是 Codex 客户端自己的 `token_count` 记账（本机约 9–10 K），而不是 provider 回报的 `input_tokens`（同轮约 34–38 K），差距约 4 倍；因此预算只把声明值当上界，实际 `model_auto_compact_token_limit` 取「实测常驻量 − 5%」，`model_context_window` 保持高于常驻量，再以 `codex exec resume <sessionId>` 执行第二轮（压缩 case 不再使用 `--ephemeral`，否则 session store 无证据）。`compaction-observed`/`compaction-not-observed` 只由隔离 `CODEX_HOME/sessions/**/rollout-*.jsonl` 中真实的 `compacted`/`context_compacted` 记录产生，模型自述、fixture 手写摘要或把摘要塞进首轮提示都不算证据；宿主无法压缩时写 capability 诊断而非伪造成功。新增 `EVAL-EXEC-COMPACT-001`（capability `execution-recovery`，suite 1.3.0→1.4.0）：过期 checkpoint 声明只完成 parse，实时 `progress.log` 已完成 parse 与 format，要求恢复后只补 verify、实读实时日志并留下 fresh 读取标记、恰好一次 `node check.js`、不得重复步骤、不得新增文件、HEAD 不得前进。`.agents/evals/suites/` 镜像与 `docs/schemas/eval-suite.schema.json` 已按生成器重放。本机实测（WSL + codex-cli 0.153.4 + deepseek-flash）：1 条真实压缩记录、隐藏测试通过、HEAD 不变、score 1、单轮约 2.5 分钟。新增测试：`tests/eval-runner.test.js` 覆盖声明 fail-closed、预算边界（含「限值必须低于宿主实测常驻量且余量足够小」）、`lastTurnInputTokens`、session store 证据（`compacted` 与 `context_compacted` 计入、模型散文与非法 JSON 不计入）、Envelope 校验，以及用假 Codex CLI 跑完整驱动链路（fixture 建 Git、第一轮不加 `--ephemeral`、第二轮带实测预算 `exec resume`、只在有真实压缩证据时才通过）；`tests/eval-execution.test.js` 覆盖 schema 拒绝畸形 `input.compaction`/`fixture.git`、observer 注册、以及「摘要不得进入 fixture 或首轮提示」。实测同时暴露的边界写入 docs/evals.md 与 TECH_DEBT：首轮上下文过小时压实后立即再次触顶，模型会在「重读→被压实→遗忘→重读」之间空转（曾观测 56 条压缩记录仍无写入），因此用例必须让首轮真实读完足够大的计划文件，并在 docs 中记录该 capability-gated 边界。
- 措辞锁定收敛为共享锚点表（审查建议项 3b，关闭 TD-2026-09-11-1）：`tests/rules-depth.test.js`、`tests/linear-workflow.test.js`、`tests/execution-simplification.test.js` 此前用 231 处 `assert.match`/`assert.doesNotMatch` 逐个匹配规则原文句子（167 处直接匹配中文），改一句规则措辞要找到并同步修改对应断言、漏改即红，合法文案调整也会被误判为规则回归。现将措辞锚点收到 `scripts/lib/pack-validation.js` 导出的单一合同表 `CONTENT_QUALITY_CHECKS`（35 个受治理文件、283 条锚点），跨文件共用的措辞另由 `SHARED_RULE_PHRASES` 定义一次（`验证受阻（degraded）`、`参考实现`、`本规则仅在`、`feat/*、fix/* → develop → main`、`Webhook 调度器、Linear Loop、leader lease、自动超时回收或自动重派` 等 11 条）；`pnpm validate` 与测试读同一张表，改写规则只需改一处常量，且失败信息按文件加锚点报出「哪条合同丢了」而不是匿名正则不匹配。锚点取「仍能承载规范含义的最短片段」（模态动词、枚举、字段集、特征标签）而非整句，普通改写不会击穿断言。测试侧新增 tests/helpers/governed-docs.js：`assertRuleAnchors()` 一次列出全部缺失锚点，`markdownTable()` 读取受治理文档的表格；三份测试保留权威存在性断言，其余转为结构性断言——三个风险档位与四个证据标签的闭集与顺序、log-management 的 5 个二级标题顺序与 ≤60 行上限、受治理规则必须继续拒绝的流程脚手架（full profile 安装、列表虚拟化阈值、跨模块强制 ADR、一任务一命名分支 worktree、tgz/npm publish），以及规则文档与机器契约的一致性：Task DAG 字段与 kind/trigger/result 与 `scripts/lib/task-dag.js` 的 `DAG_NODE_KEYS`/`NODE_KINDS`/`NODE_TRIGGERS`/`NODE_RESULTS` 逐项相同，两份任务模板的协作图列序等于 `DAG_NODE_KEYS`，Linear 投影字段表与 Linear→result 映射表的取值只能落在 `NODE_RESULTS` 内，Receipt 字段与 source/终结事件覆盖 `scripts/lib/receipt-records.js` 的 `START_RECEIPT_KEYS`/`TERMINAL_EVENT_KEYS`/`RECEIPT_SOURCES`/`TERMINAL_EVENT_TYPES`。三份测试内的 `assert.match` 由 231 处降到 25 处，其中逐字中文匹配由 167 处降到 10 处，测试数量与覆盖章节不变（301/301）。验证：`pnpm check` 301/301、`pnpm test:eval` 206 pass / 0 fail / 1 既有跳过、`pnpm eval:check`、`pnpm eval:replay`、`git diff --check` 全通过；本次只改 `tests/` 与 `scripts/lib/pack-validation.js`，未触碰 `docs/rules`、config、hooks、skills，offline reference 保持 matched，无需再生成。TD-2026-09-11-1 已按「技术债清单只记录未关闭项」的口径移出 `docs/memory/TECH_DEBT.md`。
- 已安装表面新增规则索引（审查项 4a）：`installedSurface.rulesLine` 此前只有「规则位于 `docs/rules/`」，宿主必须自己列目录才知道存在哪些专项规则、哪个文件对应哪类信号。新增 `scripts/lib/rules-index.js`，以 `manifests/rules.json`（21 条 id/source 的单一事实源）加各规则文件首个 H1 生成一行索引（`governance-core（Vibe-Harness 执行内核）、…、ast-grep（ast-grep 结构化搜索规则）`），由 install-planner 渲染进「已安装表面」；未安装 `docs/rules/` 时不渲染，manifest 条目缺 id/source、规则文件缺失或无 H1 一律 fail-closed，避免把静默缺项的索引发给宿主。同一函数被 pack-validation 的指令预算门禁复用，门禁因此按宿主真正收到的文本计量（AGENTS.md 渲染 7.4 KB、约 1.85 K token，仍在 2000 token 警告阈值内）。AGENTS.md 的受管块已按新生成结果重放，`vibe-harness validate --project .` 回到 ready；新增 tests/rules-index.test.js（索引与 manifest/H1 一致、四类 fail-closed、已安装表面在有无 docs/rules 下的渲染）并纳入 test:unit。
- 修复 i18n 提交遗留的 2 项集成测试失败：9a33ee1 把 `optionalToolFallback()` 的降级建议中文化却没有同步断言，tests/tooling-modules.test.js 仍匹配英文 `original command`，使 `pnpm test:integration`（CI 的 test:integration job）在 HEAD 上恒红——已在本机复现两处 AssertionError。断言改为语言无关的结构化契约：summary 断言每个工具块渲染自己的 `next:` 行，doctor 断言 recommendations 的 `action=fallback` 与 `phase=dependency-install`，不再锁定任何语言的文案；`node --test tests/tooling-modules.test.js` 由 2 失败转为 30/30 通过。CLI 文案自身的语言口径不统一（`unsupported` 分支与 runtime hook 诊断仍为英文）记入 TECH_DEBT（TD-2026-09-11-3）。
- 审查台账与技术债记账补齐（审查结论 C）：docs/inventory/harness-superpowers-comparison.md 新增「处置状态（2026-09-14）」台账——F-01（独立命令稳定性假阳性）、F-02（Windows 误判 Node 缺失）、F-03（Eval 指纹随换行形式漂移）标为已修复并附代码与测试证据，F-04（`stub-behavioral` 无生产者）标为已处置（文档降级加记账），F-05（真实压缩恢复证据薄）记为未处置；R-02 标为已实施、R-03 标为 deferred（资产敏感度暂由 Harness Evals RED 阶段承担）；验证记录补「复审更新（2026-09-14）」一节的实测结果，最终成熟度判断同步。docs/memory/TECH_DEBT.md 从空模板填入 6 条真实条目（TD-2026-09-02-1 `.githooks/` 不在运行时红区清单、TD-2026-09-11-1 三份测试共 231 处措辞锁、TD-2026-09-11-2 资产指纹不覆盖 adapters、TD-2026-09-11-3 CLI 文案中英混用、TD-2026-09-11-4 `stub-behavioral` 无生产者、TD-2026-09-14-1 改进候选队列为空），每条含证据、影响、owner 与关闭条件。
- 治理记忆恢复入口补上可核对状态：HEAD 的 CHANGELOG「治理记忆种子化（P2-7）」主张 docs/memory/PROJECT_STATE.md 与 docs/memory/IMPROVEMENTS.json 已填入真实状态，但两者在 HEAD 上仍是空模板。本次把 docs/memory/PROJECT_STATE.md 与 .agents/memory/CURRENT.md 填入当日真实阶段、重点、下一步与验证证据（`pnpm check` 298/298、`eval:check`、`eval:replay`、`test:eval` 206/0/1、`eval run` 资产诊断 0 且 reference matched、`git diff --check` 干净）；IMPROVEMENTS.json 保持空队列，因为候选 ID 由 `mergeImprovementCandidates` 按 type:code:targetAsset 摘要派生，手工伪造 ID 会在下次合并时产生重复条目，缺口按 TD-2026-09-14-1 记账。
- Eval 资产指纹与结账形态解耦（第一性原理审查 F-03/P0）：`scripts/lib/eval-assets.js` 直接对工作树字节做哈希，而 Git 提交时把文本规范化为 LF——同一内容在 Windows 工作树是 CRLF、在 Linux/CI 结账是 LF，指纹因此随结账形态漂移（`git ls-files --eol` 显示被指纹覆盖的文件中 60 个是 index LF / worktree CRLF：schemas 1、skills 34、.agents/skills 25），既有 reference 在干净结账上必然重放失败。新增 `canonicalAssetBytes()` 在读取层统一规范化：含 NUL 或非合法 UTF-8 的内容按原字节哈希（保持二进制字节敏感），其余文本 CRLF→LF；`scripts/harness-evals.js` 的 harness 资产哈希（`docs/rules`、`skills/core`、`templates`、`adapters`，写入报告 `fingerprint.harness.aggregateHash`）是同类的持久化指纹，改为复用同一规则。新增 tests/eval-assets.test.js 覆盖规范化语义、LF/CRLF 同指纹、真实文本变更仍漂移、二进制字节变更仍漂移四例并纳入 test:unit。已用与实现同源的算法独立复核：按 index blob（等价于全新 Linux 结账）模拟的 aggregate 与工作树规范化结果逐位相等（2cb1164e…），证明修复后指纹不再依赖结账形态。
- eval offline reference 再生成（F-03 收尾；按 test-rules「reference 更新必须单独审查并显式确认」，请在提交前审查三个 JSON 的 diff）：evals/references/vibe-harness-core.offline.json 由规范化后的真实资产指纹经 `vibe-harness eval reference --from <run> --write --force --confirm-reference-update` 再生（旧文件备份于 .vibe-harness/backups/<ts>/），evals/results/vibe-harness-core.offline.json 与 .agents/evals/references/ 镜像同步（pnpm eval:sync --write）。diff 仅含 approvedAt 与 hash 行：aggregateHash 743f838b… → 2cb1164e…，config 681b76af… → 76c74168…、rules d177d496… → 3f605a8d…、skills e18d73f5… → fd5395d6…，hooks 组（文件本就全 LF）不变；config 的差异是 CRLF→LF 假漂移的消除，rules 与 skills 另含本分支未提交的规范内容变更（docs/rules/git-rules.md、docs/rules/linear-workflow.md、docs/rules/role-routing.md 与 skills/integrations/linear-workflow/**）。再生成后 `eval run` 的 assets 诊断清零、`reference.status` 为 matched，pnpm eval:check、eval:replay 与 test:eval（206 pass / 0 fail / 1 既有跳过）全绿。
- Eval proof 合同与实际生产者对齐（第一性原理审查项 3）：`docs/evals.md` 此前把 contract-replay、stub-behavioral、online-canary 并列描述为三种可用证明，但 `stub-behavioral` 的变异评测路径已随 `refactor(eval): remove flaky behavioral eval path` 因抖动移除，仓库内没有任何生产者。文档改为逐条标注生产者与边界——contract-replay 与 online-canary 为当前可用证明，stub-behavioral 标注为无生产者、属未实现计划，资产敏感度暂由 Harness Evals 的 RED 阶段（`pnpm eval:harness run --phase red --harness-ref <旧提交>`）承担；schema `proof` 枚举保留该取值以继续读取历史资产，不做破坏性收紧。
- 指令模板启动序列收敛为单一生成源（审查项 4）：`adapters/claude/CLAUDE.template.md` 与 `adapters/gemini/GEMINI.template.md` 硬编码了 5 步启动序列、未使用 `{{installedSurface.startupLines}}`，导致生成器此前把首步措辞改为「和一个命中的专项规则」并把 memoryLoadLine 纳入序列时，两份模板静默漂移到旧的「和当前需要的专项规则」且缺 memory 步骤；现与 codex/opencode 对齐，并在 tests/cross-platform-adapters.test.js 新增用例锁定四个 adapter 渲染出的启动段落逐字一致。
- 规范精度修正（审查项 4）：`docs/rules/git-rules.md` 的「CI 只在发布边界运行」与 `.github/workflows/ci.yml` 事实不符——普通任务 PR 仍会跑 advisory CI job，只是 `develop` ruleset 不设 required status check、main ruleset 只 required `merge-gate`；改为「必须有门禁效果的 required CI 只在发布边界运行」并显式说明 advisory job 与 `main-release-gate` 的关系。`docs/rules/role-routing.md` 补未投影角色目录时的源 Prompt 路径（`roles/prompts/<role-id>.md`），消除规则与安装投影口径不一致带来的「角色不可用」误判。
- 新增 Task DAG 与 worktree 隔离校验命令，把派发前的两项重复核对固化为脚本：`pnpm task-dag check --file <dag.json> [--require-ready] [--json]`（scripts/task-dag.js，scripts/lib/task-dag.js）按 `docs/rules/ai-collab-rules.md` 的节点字段校验轻量 Task DAG——节点契约、依赖边与环、`writeScope` 路径语法与重叠、`resourceLocks`、`all_success`/`all_done` 触发语义、ready/blocked 集与确定性结构哈希（供 checkpoint 记录）；错误 fail-closed：未知前驱、自依赖、环、绝对路径或 `../` 或复杂 glob 的 writeScope、read 节点带写范围、write 节点无写范围、共享路径或锁且无依赖路径的两个 write 节点都不 ready，`blocked` 保持非终态（依赖它的 `all_done` 节点只等待、不报失败）。`pnpm worktree list|check|plan`（scripts/worktree.js，scripts/lib/worktree-audit.js）解析 `git worktree list --porcelain -z` 并核对隔离单元：worktree 必须在仓库外部且不互相嵌套、非主工作区必须绑定命名分支、同一分支不得被两个 worktree 占用、write 任务分支必须是 `<type>/<ISSUE-ID>-<slug>`（默认路径为仓库同级 `<repo>-worktrees/<ISSUE-ID>`），并报告 merge-back 事实（分支是否并入 `--base-ref`、merge-base 是否偏离冻结 `--base-sha`）与 `--deep` 的未提交改动；未创建、未纳管、未 merge-back、脏工作区以警告报出并可经 `--strict` 升级为失败。两个命令都只读：不写文件、不执行 `git worktree remove`/`prune` 或删除分支，未合并 worktree 只阻止宣称“已集成”，`cleanupAllowed` 只是人读建议。新增 tests/task-dag.test.js、tests/worktree-audit.test.js 并纳入 test:unit；CONTRIBUTING.md 补使用指引。
- 执行内核强化为“获取可信事实 → 判定并执行 → 聚焦验证 → 简洁交付”：新增按快速/轻量/完整档匹配证据强度的事实充分性判定与四类歧义路由，不要求机械双来源；完成的 Plan 必须显式给出直接实施、拆分实施或拆分并声明依赖的人读判定，且判定不扩大写入授权。同步 README、贡献指南、adapter 投影、任务模板和行为校验，并新增 EVAL-FACT 与澄清边界用例。
- 多角色 Agent 升级：新增七个规范化角色、权限预设、确定性 role-projection 编译器和 roles:audit；full 默认启用，其他 profile 可显式启用。生成 Codex、Claude、Gemini、Cursor、Qoder、Antigravity、OpenCode 原生 Agent 与 ZCode 项目插件，接入 state v5 owner、冲突、升级、回滚和卸载生命周期；ZCode 需要手动激活，权限表达不足的宿主报告降级映射。新增角色路由在线 Eval 与宿主生命周期测试；未完成真实 canary 或工作区未隔离时保持 No-Go。

- eval offline reference 按内容变更再生成（B5-d，需维护者提交前审查 diff）：evals/references/vibe-harness-core.offline.json 与 evals/results/vibe-harness-core.offline.json 经确定性重放（buildOfflineRun）再生，.agents/evals/references/ 镜像同步。diff 范围仅含预期漂移组：config（B4-a install map 红区补标）、hooks（P1-5 hook 启动提速复用 git root）、rules（B2 拆段与 B3 跨文件收敛）、skills（B4-b description 改写，文件数 99→100 为 2026-08-18 892ee58 新增 linear-workflow 参考文件——上一次发布未再生成 reference，approvedAt 停留在 2026-08-12，本次一并追平）；aggregateHash 与各组 hash 相应更新，run status/overallScore/criticalPassRate 保持 passed/1/1。此前 test:eval 中 2 项指纹断言失败随本次再生成消除，pnpm eval:check 与 pnpm test:eval 全绿（152 pass / 0 fail / 1 既有跳过）。按 test-rules「reference 更新必须单独审查并显式确认」，请在提交前审查三个 JSON 的 diff。
- 治理记忆种子化（P2-7）：docs/memory/PROJECT_STATE.md 从空模板填入真实状态（P0/P1 已落地、P2 五批次进行中、B5-d reference 再生成为交付确认项，恢复提示指向审查报告、TECH_DEBT 与改进队列）；docs/memory/IMPROVEMENTS.json 按 schemaVersion 1 填入 P2-1..P2-10 十个候选（9 项 implemented、P2-10 Claude permissions.deny 兜底为 eligible-for-owner-review 待 owner 评审，ID 按 improvements-audit 的 type:code:targetAsset 摘要派生）；.agents/memory/CURRENT.md 同步为当前真实状态并带当日验证日期。自安装校验（validateSelfInstalledArtifacts）要求无占位符的 replace 源与安装副本逐字一致，直接改 docs/memory 会漂移——按 TECH_DEBT 模板既有先例给 templates/memory/PROJECT_STATE.md、memory/CURRENT.md 补渲染说明注释（{{projectName}} 占位符，安装时渲染、副本各自维护），templates/memory/IMPROVEMENTS.json 补 "project": "{{projectName}}" 字段（schema 同步新增可选 project 属性，mergeImprovementCandidates 重写时保留该字段）；docs/memory/ARCHITECTURE、FAILURE_LEARNINGS、KNOWN_BUGS 三个仍为纯模板（暂无真实内容可填，未动）。已用 validateJsonAgainstSchema 校验队列零错误、auditMemory 对本仓库运行通过。AGENTS.md 启动序列不变。
- EVAL-SPLIT fixture 一致性护栏：tests/eval-contract.test.js 新增契约测试，锁定 canary 套件三个 EVAL-SPLIT 用例（plan-task-split）内嵌的 AGENTS.md fixture 文本必须逐字一致（三处此前为独立复制，存在漂移风险），并锚定硬触发与 4+ 阈值关键句；docs/evals.md 注记该 fixture 文本派生自 governance-core 拆分判定段（第三条硬触发「探索加实现预计超出单次上下文」与软信号 `multi test layers` 属意译），更新 governance-core 拆分判定段时需同步 fixture。不引入 fixture 自动生成（生成器需中→英翻译）。
- 新增治理资产生成命令，把重复的文档、ADR 与镜像维护动作固化为脚本：`pnpm docs:sync`（scripts/docs-sync.js）把 docs/ 受治理文档补进 docs/catalog.json、docs/adr/catalog.json、docs/memory/DECISIONS.md、docs/README.md 与 docs/archive/README.md，并把 docs/schemas/ 渲染副本按 schemas/ 逐字重放；`pnpm adr:new --title <标题> --owner <owner>`（scripts/adr-new.js）按 templates/adr/adr-template.md 生成下一个 ADR 并调用同一投影补齐 catalog、DECISIONS 与索引；`pnpm eval:sync`（scripts/eval-sync.js）按 adapters/install-map.json 重生成 .agents/evals/ 镜像；`pnpm release:readiness`（scripts/release-readiness.js）核对版本一致性、changelog 条目与可选的发布边界事实并输出就绪收据。四个命令默认只读、只追加缺失记录、不重写已有条目，需要 owner 判断的悬空、未纳管、未分类路径与孤儿镜像项以 manual action 报出（退出非零），真实写入统一加 `--write`。新增 tests/docs-projection.test.js、tests/adr-scaffold.test.js、tests/eval-projection.test.js、tests/release-readiness.test.js 并纳入 test:unit；CONTRIBUTING.md 补使用指引。
- 新增执行信封与回执登记命令，把 Execution Envelope 与 Linear 交接协议中可机械化的部分固化为脚本：`pnpm envelope plan`（scripts/envelope.js，scripts/lib/envelope-records.js）从当前工作区身份生成 v2 草稿——canonical cwd、worktree root、Git common dir、git dir、branch、冻结 base ref/SHA、initial HEAD、allowed write roots、无凭据 external targets 与 mode effect 上限都由命令算出，`requestId`、`sessionId`、`hostContext` 只作为宿主注入字段留空并在输出中标记缺失；命令不自造宿主证明，草稿在宿主补齐前保持 invalid（宿主的 filesystem/approval/process/network 证据只能经 `--host-context <file>` 传入）。`pnpm envelope check` 用发布 JSON schema、runtime `parseExecutionEnvelope` 与当前工作区复核：越界 effect、allowed/forbidden 冲突、过期、陈旧或非宿主来源的证明、工作区身份与 HEAD/base ref 漂移、checkpoint 失配、cwd 非 Git 工作区都 fail-closed 报错并退出非零；mode effect 上限以行为测试与 runtime 的 `EXECUTION_ENVELOPE_MODE_VIOLATION` 判定逐 mode 对齐。`pnpm receipt start|event|handoff|check`（scripts/receipt.js，scripts/lib/receipt-records.js）固化 `vibe-harness.linear-execution/v1`、`vibe-harness.linear-execution-event/v1` 与 `vibe-harness.handoff/v1`：生成新 executionId/runtimeInstanceId（不复制宿主 thread、session、用户名、主机名或路径）、handed-off 事件强制携带预生成 successor ID、handoff 载荷只有 accepted 且 finalCheck 为 reviewed/passed 才能声明 complete，`check` 对一个 Issue 的评论历史做冲突分析（同 ID 不同内容、同一 Issue 多 active execution、sensitive 字段、孤儿或重复终结事件、successor 的 source 或目标 Issue 不符 fail-closed；交接未确认、进度交接等合法中间态以 pending 报出），并对用户名、主机名、本地路径、Token、Cookie、邮箱等结构化作脱敏拦截，只报告位置不复制匹配文本。三个生成命令默认只打印记录，`--write` 时才写入 `--out`，且都不写入 Linear。新增 tests/envelope-records.test.js、tests/receipt-records.test.js 并纳入 test:unit；CONTRIBUTING.md 补使用指引。
- 门禁接线修复：孤儿脚本 branch-policy.js、independent-review.js 与 check-pull-request-approval.js 接入 CI 的 pull_request 事件并聚合进 merge-gate（main ruleset 的唯一 required check），merge-gate 的 needs 与 REQUIRED_* 变量同步补齐；independent-review 与 high-risk-approval 默认 shadow，设置 VIBE_HARNESS_INDEPENDENT_REVIEW_MODE 或 VIBE_HARNESS_PR_APPROVAL_MODE=required 后才转为强制。merge-gate.js 移除从未被任何 job 导出的 DEVELOP_GATE_RESULT 与 MAIN_RELEASE_GATE_RESULT 死变量，改为读取真实存在的 HIGH_RISK_APPROVAL_RESULT；branch-policy.js 与 check-pull-request-approval.js 的入口判断改用 pathToFileURL，修复 Windows 上 CLI 静默不执行的问题；release-verify 在安装依赖前运行 pnpm release:readiness --sha --require-clean 并把收据写入 release-artifacts/。新增 tests/ci-gates.test.js 覆盖聚合与 shadow/required 行为。
- eval:behavioral 变异覆盖补齐为「每控件每个 required 片段独立变异」：变异数由每控件仅变异第一个片段（共 4 个）补齐为全部片段独立变异（共 10 个：2+2+3+3），变异 ID 带片段序号并在报告中记录被移除片段文本；变异检测改为诚实语义——控件本已失败时不再空真报告 detected（仅在观察到由通过翻转为失败时计检出），status 判定不变。新增 tests/eval-behavioral.test.js 并纳入 test:unit：真实包锁定每控件变异数与全部检出、全片段 fixture 通过、缺失非首片段时对应 case 失败且该控件变异不空真。
- Skill description 语言护栏与统一（宿主按 description 文本路由）：pack 校验新增两层语言一致性检查——单条 description 不得中英混写（内嵌技术词如 $git-deliver、Playwright 可容忍）、全部 Skill 的 description 必须同用一种文字；git-deliver 与 browser-verification 的 description 由中文改写为英文（正文保持中文），git-deliver 的 Codex openai.yaml 元数据同步英文化（原为 9 个原生 Skill 中唯一中文元数据）；eval-driven-development 的 description 补负向边界 `not deterministic logic, bug fixes with known causes, or deterministic refactors`。原生 Skill 身份预算由 1100 字符重校准为 1300：英文 description 同等信息量下字符数更高但 token 占用更低（1300 英文字符约 330 token，原中文 1100 字符约 625 token），预算意图是上下文经济性而非字符数本身。skills/ 与 .agents/skills/ 双份同步；manifest-schema 测试新增真实包全英文断言与混写/分裂/全中文三类构造用例。
- 红区三清单修齐并新增交叉校验（安全语义）：RED_ZONE_PATTERNS 移除从未生效的 `.gemini/` 死正则（gemini 安装面仅含 `.gemini/skills/` 技能内容，runtime 清单与 redZonePrefixes 均无 `.gemini/`），补入 `vibe-harness.config.json`、`.vibe-harness/install-state.json` 与 `.agents/runtime/hooks/` 三个控制面模式；install map 的 8 条 `.agents/runtime/hooks/**` 条目补标 redZone。行为变化：含 hooks 组的安装（尤其 gemini full，此前因无任何红区条目可免确认写入 runtime hook 脚本）现在统一要求 `--confirm-red-zone`。新增 `validateRedZoneConsistency` 三向校验（安装目标命中运行时红区必须被门禁 / 运行时路径必须有安装期门禁 / 正则条目必须覆盖至少一条真实路径，防死正则再积累），接入 `pnpm check`；新增 `tests/red-zone-consistency.test.js` 覆盖三类漂移回归。`.githooks/` 不在运行时清单的观察记入 TECH_DEBT（TD-2026-09-02-1）。
- 跨文件规范重复收敛 4 组（rules/ 与 docs/rules/ 双份同步）：git credential helper 由 git-rules 保留完整定义、linear-workflow 改引用；Envelope mode/effect 枚举统一措辞并为 git-rules 的领域性缺项（linearWrite）补互见；checkpoint 字段清单由 governance-core 保留完整、linear-workflow 改引用式并保留其 Git/PR/MR 状态核对特有内容；writeScope 路径验证由 ai-collab-rules 保留完整定义、linear-workflow 保留 Linear 投影特有部分。
- governance-core 默认循环的 1039 字符巨型单段拆为三个编号子项（直接执行与最小改动 / 拆分判定 / Envelope 模式与枚举），原句文本逐字保留；措辞锁定收敛为「同一句规则文本只被一个测试文件锁定」：rules-depth 保持权威存在性断言，execution-simplification、linear-workflow、mvp-spec、project-profile 中重复的中文措辞断言删除或改为结构性断言。
- 测试断言与套件对齐：canary 套件断言对齐实际状态（risk 允许 critical/high 且 critical 占比 ≥ 80%，workflowDemand 对齐套件 7 项排序）；删除 linear-workflow.test.js 中断言 `.github/workflows/hotfix-back-sync.yml` 的死测试块（该文件在全部 git 历史从未存在）；上轮审查遗留项（hotfix-back-sync、负载敏感 flaky、并行度标准、reference 更新流程）记入 TECH_DEBT。
- 修复 Windows 上 `vibe-harness verify` 执行 pnpm/npm/yarn 验证命令的 EINVAL 失败：.cmd 垫片统一经 `cmd.exe /c` 启动（对齐 git-hook 既有模式），并新增 Windows 回归测试。
- 行为正确性修复：install 增加未知选项白名单并在 usage 补 `--provision`；删除 rollback/uninstall/provision 三处不可达的重复 target 检查；diff/rollback/uninstall/baseline 输出统一经公共报告脱敏（绝对路径不再进入 stdout）；单 target uninstall 部分失败时 remainingState 同步移除该 target 并写入 configUpdate；install 对全部所选 adapter 的指令模板渲染做校验（此前仅第一个）。
- 规范对齐：governance-core 的失败绕过条款升级为「降低断言、删除断言或无理由跳过相关测试」与 test-rules 同精度（双份同步）；AGENTS.md 已安装表面补记 browser-verification integration Skill。
- 流程提速：test:integration 由串行改为 --test-concurrency=2（26 个测试文件经并行安全审计：动态端口、pid 隔离 npm cache、mkdtemp 临时目录、文件级子进程隔离），全量实测 1691s/1521s（两次并发运行），较串行基线 4288s 提速约 2.5–2.8×；validate 等命令向 diffMultiTargetInstall 传入已构建安装计划消除二次全量规划；pack 静态 manifest/schemas/install-map 读取改经 readPackJson 进程级缓存（目标项目可变状态保持直读，避免同进程读改写污染）；多 target 规划改 Promise.all 并行；Hook bootstrap 定位的 git root 经环境变量传递给 hook，消除每次 guarded 工具调用的重复 git 子进程。
- 测试规则按社区实践补强：失败不得通过降低断言、删除断言或无理由跳过相关测试绕过；快照/golden baseline 与 Eval reference 统一「更新须单独审查显式确认」；新增覆盖率是诊断信号不是目标、缺陷修复先写复现测试、flaky 隔离限期修复（轮询替代 sleep）、断言行为而非实现细节且不引入仅测试使用的辅助路径；eval:replay 表述修正为契约重放（contract-replay，不执行当前 Agent 规则、Skill 或 Hook），eval:behavioral 变异验证写入测试与 Eval 边界节；关键句同步纳入 rules-depth 机器锁定。
- `pnpm test` 改为按序聚合 test:unit、test:eval、test:integration，修正原先以并发 4、超时 30 秒运行含集成测试全部文件的问题；managed-json-config、execution-observer-parity 纳入 test:unit，safety-posture 纳入 test:integration，消除三个脱离 CI 的测试文件；AGENTS.md 与 CONTRIBUTING.md 验证矩阵补充 `pnpm test:eval`。
- 执行内核默认循环新增计划拆分判定：计划完成后不默认直接执行也不默认拆分，先按硬触发（契约/schema/数据模型迁移、混合重构与行为修改、单次上下文装不下或单 Agent 无法稳定完成）加软信号计数（多模块、前后依赖、可并行、多验收阶段、多层测试）两级判定，0–1 项直接执行、2–3 项拆分、4 项及以上必须拆分并声明依赖；实施任务须可独立执行、独立验证、独立提交，操作步骤不得作为任务。
- 任务模板（中英文）新增可选「实施任务拆分」表；canary 套件升级至 2.8.0，新增 EVAL-SPLIT-001..003 覆盖直接执行、硬触发拆分与必须拆分带依赖三档判定；capability 目录注册 plan-task-split。
- 新增第八个正式 target OpenCode：共享根 AGENTS.md 受管块，安装原生 .opencode/skills，支持完整项目生命周期和项目级 MCP；不安装 OpenCode plugin Hook，并显式报告 DEGRADED_SAFETY_POSTURE。
- OpenCode 项目配置确定性选择 opencode.json 或 opencode.jsonc，使用固定 jsonc-parser 3.3.1 保留注释、尾逗号、格式和用户键；只管理 mcp.vibe-harness-*，并将两种配置文件纳入安装与运行时红区。

- Breaking change：项目配置升级为非空 targets 数组，install-state 升级为 stateVersion 5 并记录 shared 与 adapter 所有权；旧 target/state v4 仅在标准 upgrade write 事务中持久化迁移。
- 新增 Codex、Claude Code、Gemini CLI、Cursor、Qoder、ZCode 和 Antigravity 的单项目多宿主安装；公共 runtime、memory、Eval、工具 provisioning 与项目根索引只维护一份。
- 新增目标级卸载、all-targets 完整卸载、stale projection 报告、嵌套旧安装 doctor 检测，以及逐宿主 stable、preview、unsupported、skipped 和 conflict 状态。
- Antigravity rules、Skills 和 MCP 进入 stable；Hooks、sandbox 和 memory 保持 preview，Hook 协议支持 camelCase 输入、四种 decision 和高风险 fail-closed。

- **Breaking change**：产品更名为 Vibe-Harness。主 CLI、npm 包、配置文件、状态目录、环境变量和受管标记分别改为 `vibe-harness`、`@jw/vibe-harness`、`vibe-harness.config.json`、`.vibe-harness`、`VIBE_HARNESS_*` 和 `VIBE_HARNESS` 。
- **Breaking change**：旧品牌资产不提供兼容或迁移；检测到旧配置、状态目录或受管标记时以 `VIBE_HARNESS_LEGACY_UNSUPPORTED` 拒绝执行。
- **Breaking change**：删除治理链配置、CLI workflow 参数、任务 schema/runtime、审查角色、收据、完成门禁和对应 Hook 事件；旧配置以 `VIBE_HARNESS_OBSOLETE_GOVERNANCE_CONFIG` 只读拒绝。
- 默认执行统一为“获取事实 → 直接执行 → 聚焦验证 → 简洁交付”；风险档位只影响审批和验证范围。
- `vibe-harness verify` 顺序执行可选 `lint`、`typecheck`、`test`、`eval`；`validate` 只检查安装一致性。
- `full` 重定义为全部领域 Skills、可选 Eval 和 Codex 安全 Hook；Codex Hook 只保留 `PreToolUse` 与 `PermissionRequest`。
- 升级器会安全退役新计划不再包含的旧受管文件，并精确清理旧运行状态而保留其他 `.vibe-harness` 数据。
- `pnpm check` 收敛为 lint、pack/catalog 静态校验和快速产品单测；integration、lifecycle 与在线 Eval 保持显式命令。
- 新增显式 `--plugin` 安装面：`core` 与 `full` 默认均不安装外部工具；`-all`、单选、多选和 `none` 分别管理 RTK、ast-grep、codebase-memory-mcp、Chrome DevTools MCP、Playwright CLI 与 Open Code Review 共 6 个插件，并将规范化选择持久化到 install-state。Agentmemory runtime 因上游 High 漏洞暂停提供，不通过 `--plugin` 安装。
- 新增项目内 RTK `v0.43.0` 与 `@ast-grep/cli@0.44.1`：提供命令输出压缩、结构化搜索规则、checksum/lockfile 校验、doctor 状态和安全回退。
- docs(readme)：默认 README 改为中文（根 `README.md` 渲染中文），英文版移至 `README.en.md`，并删除 `README.zh-CN.md`；同步更新 `validateReadmeParity` 签名与错误标签、`manifests/capabilities.json` 文档清单、`docs/catalog.json` 条目以及 `docs/README.md` 链接。

## 0.5.0 - 2026-07-18

- 产品正式命名为 Cognis（智序，旧称 LoopEngine），package、主 CLI、配置、活动 runtime、Skill、评测及产物统一使用 Cognis 命名。
- 新增渐进兼容层：旧 CLI、配置、状态根、受管标记和环境变量继续可读；`install --upgrade` 以事务方式迁移配置并退休未修改的旧品牌资产。
- install state 升级为 stateVersion 4，记录 `product` 与 `storageNamespace`；旧安装升级后保留 `.loopengine/` 状态根，支持 recover、rollback 和 uninstall。
- 新增旧品牌 allowlist 审计，旧名称仅允许存在于兼容实现、迁移说明、兼容测试、归档和审计记录。

- 加固 Codex Hook 的破坏性 Git 与跨平台全局配置策略，Stop validator 缺失改为 unavailable，交付解析忽略示例和占位内容。
- 能力目录升级为 schemaVersion 2，Eval 变更门禁要求 capability 与 suite 精确匹配；online canary 增加隔离 HOME 配置写观测和连续 degraded 告警。
- 新增 `runtime:audit`，按真实 provision 参数阻断 Critical/High；`skills:audit` 改为真实 Skill 图审计，lint 覆盖所有分发脚本，CI 增加 Windows required matrix。
- 新增文档 Catalog、历史归档索引和 `docs:audit` 机械校验，并将文档漂移门禁接入 `pnpm check`。
- 将常驻治理收敛为“获取事实 → 做出决策 → 执行 → 验证 → 交付”，删除独立 workflows catalog 和重复生命周期规则。
- 任务真值改为中文 Markdown；完整档位使用中文 JSON 控制块和独立核验门禁。
- 新增 `using-cognis` router，模板随 skill 渐进披露；minimal/docs-only 使用短治理内核降级。
- 统一所有项目命令为 `--project` + `--write`；移除 `--apply`、路径型 `--target`、`codex-internal` 和 `codex-minimal`，旧 install-state 在标准升级时归一。
- task schema 和 full governance validator 增加 AC-ID、验收证据、责任人、确认状态、artifact 存在性和可声明时间盒门禁。
- Review Packet 对未修复的 Medium finding 强制要求有 owner、关闭条件和批准者的结构化延期。
- 完整档位任务新增 Red Team 完成门禁、结构化审查包和独立审查者校验；该能力随 core profile 安装，旧开放任务在完成前迁移。
- 新增 `cognis verify --project`，显式执行目标项目配置的验证命令，同时保持 `validate --project` 只读。
- 新增 `cognis baseline --project`，在安装后生成可比较的 JSON 快照和 Markdown 工作流建议，并支持可选验证与受管回滚。
- 新增 Failure、Retrospective 模板、core/full smoke script 和 GitHub Actions CI。
- Chrome DevTools MCP 新增为项目内受管工具：`full` 和显式 `chrome-devtools` 模块固定安装 `chrome-devtools-mcp@1.6.0`，以系统 Chrome 的无头隔离模式执行 console、network、Lighthouse 和 performance 诊断；`browser-verification` 使用“DevTools 定位、Playwright 回归”，两者不可用时再回退人工浏览器步骤。
- `codebase-memory-mcp` 安装改用稳定 flags 执行 moderate 索引，并以项目根目录匹配的 `index_status` ready 结果作为工具就绪门禁。
- 将六个 Agentmemory 薄 skill 收敛为单一入口和按需 references；升级器新增显式、hash 保护且可回滚的旧入口退役动作。
- 删除八个重复或薄包装 skill；OCR、契约、前端实现、任务拆解和交接职责并入 canonical skill 或规则，core/full skill 数量收敛为 13/18。

## 0.3.0

- 新增机器可校验的治理能力覆盖矩阵。
- 新增安装到目标项目的零依赖 basic/full 治理校验器和 Packet 校验器。
- task schema 增加恢复、阻塞、档位升级、verifier 和结构化跨仓证据字段。
- lint/typecheck 配置改为可选，`validate --project` 静态报告命令可用性且不执行用户命令。
- 深化通用治理规则并移除悬空来源引用。

## 0.2.0

- 新增 `.loopengine/install-state.json` 安装状态记录。
- 新增 `loopengine diff`、`install --upgrade` 和 `loopengine rollback`。
- 新增 MVP 项目模式：`loopengine init --project`、`install --project <path> --target codex --profile minimal|core|full --write`、`validate --project`。
- 新增 `loopengine.config.json`、Codex `AGENTS.md` 模板渲染和 `minimal` / `core` / `full` profiles。
- `validate --project` 现在会检查目标项目已安装文件是否与所选 profile 匹配。
- MVP `AGENTS.md` 安装改为受管块更新：保留目标项目原有内容，不默认修改 Node / pnpm 元文件。
- CLI 运行时错误改为结构化 JSON 输出，避免暴露 Node stack trace。
- Pack validation 接入 JSON Schema 校验，避免 schema 文件仅作为静态摆设。
- 强制覆盖或升级前生成目标项目本地备份。
- 回滚红区文件时要求显式 `--confirm-red-zone`。

## 0.1.0

- 初始化 Codex 优先的内部治理包。
- 新增规则、模板、核心 skills、workflows、manifests、Codex adapter、dry-run 安装器、校验器、测试和示例。
- 收口 CLI 语义：默认 dry-run，真实写入使用 `--apply`，红区写入使用 `--confirm-red-zone`。
- 新增目标项目安装状态校验、manifest/install-map 结构校验和发布前 smoke 检查说明。
