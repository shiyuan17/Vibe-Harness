# 执行模型收敛记录

当前执行模型只保留四个原则：

1. 从仓库、配置和公开契约获取事实。
2. 对清晰、已授权、可逆的本地工作直接执行。
3. 验证范围与完成主张匹配。
4. 只报告结果、实际变更和本轮验证。

快速、轻量、完整是风险标签，不是状态机。安全审批、工作区保护和证据真实性保留；任务文档是可选的人读记录。领域 Skills 与显式工具只在需求命中时使用。

历史执行模型、规格、任务和审查材料位于 `docs/archive/`，不作为当前行为合同。
