# 技术债

记录未关闭技术债，包含 ID、证据、影响、owner 和关闭条件。

| ID | 技术债 | 证据 | 影响 | owner | 关闭条件 |
| --- | --- | --- | --- | --- | --- |
| TD-2026-09-01-1 | hotfix-back-sync 工作流缺失：`tests/linear-workflow.test.js` 曾断言 `.github/workflows/hotfix-back-sync.yml`，但该文件在全部 git 历史中从未提交，断言属死代码（已于 2026-09-01 删除）。若未来恢复 hotfix 回同步流程，需补工作流文件与配套断言。 | `audit-reports/2026-09-01-governance-workflow-review.md` §7.1；`git log --all -- .github/workflows/hotfix-back-sync.yml` 为空 | 曾长期导致 test:integration 1 个固定失败；hotfix 分支的 main→develop 回同步缺自动化保障 | maintainer | 决定恢复该流程时提交工作流文件并恢复断言，或确认永久放弃后从本表移除 |
| TD-2026-09-01-2 | 集成测试存在负载敏感型 flaky：MCP browser probe（握手超时）与 full write degrades（120s 超时取消）两类失败在串行与并发（--test-concurrency=2）全量下均出现，与并发度无关，属环境负载敏感。2026-09-02 复测归因：记录宿主 CLI 单次开销实测 init≈3.4s、install≈27.7s、validate≈48.6s，两类失败均伴随宿主高负载出现，与代码路径无关。 | `audit-reports/2026-09-01-governance-workflow-review.md` §6 验证记录、§7.2；P2 执行会话（2026-09-02）CLI 计时记录 | 全量验证结果不稳定，需要人工区分回归与 flaky | maintainer | CI 对这两类失败提供重试机制或显式标记（如 node:test retry），连续 10 次全量运行无此类失败后移除标记 |
| TD-2026-09-01-3 | 并行度提升判定标准未量化：test:integration 已从 `--test-concurrency=1` 提升到 2（4288s→1691s），是否可再提到 4 需两连绿基线与 CPU/磁盘争用观测。 | `audit-reports/2026-09-01-governance-workflow-review.md` §6、§7.3 | 提速空间未定，盲目提并发可能引入新 flaky | maintainer | 并发 2 全量两连绿后试跑并发 4，对比耗时与失败集，确认无新增失败后再默认启用 |
| TD-2026-09-01-4 | eval reference 更新需维护者显式确认：规则/钩子内容的任何变更都会使 evals/references 的 asset fingerprint 按设计漂移；`--confirm-reference-update` 入口已存在，但缺少书面的审查清单（diff 范围应只含预期变更组）。 | `docs/rules/test-rules.md` reference 审查条款；本轮 P2 批次执行时 hooks/rules 组指纹漂移 | fingerprint 漂移易被误当回归处理，或被无审查地批量再生成 | maintainer | 在 CONTRIBUTING 或 test-rules 中补充 reference 更新的审查清单模板并引用一次实际更新作为范例 |
| TD-2026-09-02-1 | 安装期 git hooks（`.githooks/pre-commit`、`.githooks/pre-push`，install-map hooks 组 2 条目）不在运行时红区清单 `DEFAULT_RED_ZONE_PATHS` 中：安装器写入它们走 red-zone 门（已标记 redZone），但 Agent 运行时写门不拦截对 `.githooks/` 的后续修改。 | `adapters/install-map.json` hooks 组条目；`runtime/hooks/lib/context.mjs` DEFAULT_RED_ZONE_PATHS 无 `.githooks/`；`tests/red-zone-consistency.test.js` 三向校验按运行时清单判定故不报错 | Agent 理论上可在安装后改写 git hooks 脚本而不触发红区拦截；属纵深防御缺口而非已知漏洞 | maintainer | 评估将 `.githooks/` 加入 DEFAULT_RED_ZONE_PATHS 与 RED_ZONE_PATTERNS 的成本（需同步 project-config 默认清单与既有项目），或论证 git hooks 由仓库自身保护即可后关闭 |
| TD-2026-09-02-2 | typecheck（`tsc -p jsconfig.json`，noEmit）从未纳入任何自动验证，基线积累 143 处历史类型标注问题（TS2339×81、TS2353×28 等，29 个文件；较集中：scripts/lib/eval-report.js×19、scripts/lib/project-profile.js×16、scripts/vibe-harness.js×12、runtime/hooks/lib/execution-envelope.mjs×12）。修复需逐处理解 JSDoc 契约，属独立批次。当前处理：CI 以 `continue-on-error` 非阻断运行 typecheck 使信号可见；`vibe-harness.config.json` 的 validationCommands.typecheck 暂保持 null（启用会使 `verify --project` 被基线错误卡死），AGENTS.md 托管块 "Typecheck: 未配置" 与现状一致。 | `pnpm typecheck` 2026-09-02 实测 143 errors；`pnpm lint:eslint` 同日实测 0 errors / 80 warnings | 类型回归当前不可见（非阻断）；verify 验证链缺 typecheck 维度 | maintainer | 类型错误清零（建议按文件分批修复）→ ci.yml typecheck 步骤转为阻断 → 在 `vibe-harness.config.json` 启用 `validationCommands.typecheck` 并重渲染 AGENTS.md 托管块 → 从本表移除 |
