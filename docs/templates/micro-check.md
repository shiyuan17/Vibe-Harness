# Micro Check

- `id`：
- `kind`（module/rule/pure）：
- `entry`（项目相对路径）：
- `args` / fixture：
- `costTier` / `scopes`：
- `maxDurationMs` / `maxOutputBytes`：
- `deterministic` / `network` / `workspaceWrite`：
- `allowedEnv` / `expectedExitCodes`：
- `promotableTo`：
- 局部不变量与输出摘要：
- 失败升级层级：
- 不可支撑的完成主张：
