# 实施计划模板

填写规则：标记“必填”的字段不得留空；禁止空泛词（待定、视情况、合适测试、后续补充）。计划必须决策完整，执行者不需要再猜文件、接口、顺序或验证命令。

## 摘要

必填。说明本计划实现哪份 Spec/Task、采用什么路径、最终可交付物是什么。

## 任务模式与拆分判断

必填。声明任务模式：`single` / `parent` / `child`。

- 拆分结论：
- 判断依据：
- 最小可审查闭环：
- 是否存在过细拆分：
- 5 分钟判断：

小任务单一目标、单一写入范围、一次验证闭环即可完成时保持 `single`。需要多 agent、多个 worktree、完整流程（`Full`）、高风险、多个可独立审查交付物，或写入范围可并行拆分时，使用 `parent` / `child`。

## 父任务统筹

parent 计划必填；single 或 child 写“不适用”。

- 子任务列表：
- 依赖顺序：
- 并行安全：
- 冲突文件 / 冲突关系：
- 集成验收：
- merge-back 策略：

## 子任务执行

child 计划必填；single 或 parent 写“不适用”。

- 父任务 ID：
- 5 分钟目标：
- 子任务验收标准：
- 子任务非目标：
- 写入范围：
- 禁止动作：
- 验证命令：
- 交付证据：
- 停止条件：
- 回滚方案：

## 动态实施笔记

必填。声明是否维护 `implementation-notes.md`；完整流程（`Full`）或红区任务必须维护，轻量流程（`Lightweight`）可写“不维护，原因：...”。

- 记录位置：
- 维护要求：
- 偏离说明规则：遇到边缘 case 时选择最保守备选，记录偏离说明、影响范围和验证证据，再继续最小安全路径。

## 关键改动

必填。按子系统或行为列出改动，每项说明目标文件范围、依赖顺序和回滚方式。

## 公开接口 / 类型

必填。列出 API、CLI、schema、配置、模板、类型或文件格式的变更；无变更时写“无公开接口 / 类型变更”。

## 测试计划

必填。列出每个验收点对应的验证命令、预期结果和失败时处理方式。

- 验证命令：
- 预期结果：
- 覆盖的验收标准：
- 未覆盖项：

## 假设

必填。只记录已确认或可接受的假设；任何会改变实现路径的未知项必须回到澄清阶段（`Clarify`）。
