# 决策记录

只记录非派生、未来仍需要的长期决策。格式建议：

- 日期：
- 决策：
- 背景：
- 为什么（Why）：决策的理由，用于判断边界情况
- 如何应用（How to apply）：应用范围和注意事项
- 影响范围：（可填 glob 路径，如 `src/api/**/*.ts`，供 codebase-memory-mcp 的 detect_changes 交叉比对）
- 复核方式：
