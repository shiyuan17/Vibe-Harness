interface:
  display_name: "Clarify Requirements"
  short_description: "Clarify blockers or discover decision-ready requirements"
  default_prompt: "Use $clarify-requirements to resolve blocking product decisions or conduct explicit requirements discovery."
policy:
  allow_implicit_invocation: true
