---
name: task-intake
description: 用于在实现前将粗略请求整理为可执行任务启动块。
---

# 任务启动

先阅读可用项目上下文，再输出精简任务启动块：目标、验收标准、非目标、风险、workflow、验证方式和停止条件。

## 触发条件

当请求还没有稳定 task 对象，或需要明确范围/风险时使用。

## 输出

使用 `templates/task-intake.md`。

## 禁止项

验收标准不清或红区确认缺失时，不得开始实现。
