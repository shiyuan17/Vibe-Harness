interface:
  display_name: "Git Deliver"
  short_description: "仅在显式调用时，分组提交并安全推送当前任务相关改动"
  default_prompt: "使用 $git-deliver 将当前任务改动按逻辑分组为经过验证的 Conventional Commits，并普通推送当前分支。"
policy:
  allow_implicit_invocation: false
