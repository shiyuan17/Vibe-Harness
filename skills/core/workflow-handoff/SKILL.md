---
name: workflow-handoff
description: 在暂停、转交或带未完成事项结束工作时创建 handoff。用于用户要求 handoff、session summary、保留下步上下文，或工作结束仍有剩余事项。
---

# 工作流交接

读取 `docs/rules/handoff-rules.md`，使用 `docs/templates/handoff-template.md`。填入当前仓库事实、验证退出码、Git/worktree/merge-back 状态和可直接执行的恢复提示；不要把聊天流水账当成交接。
