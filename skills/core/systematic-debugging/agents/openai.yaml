interface:
  display_name: "Systematic Debugging"
  short_description: "Prove a specific failure's root cause—not a bug survey"
  default_prompt: "Use $systematic-debugging to reproduce this failure, prove its root cause, and verify the smallest fix."
policy:
  allow_implicit_invocation: true
