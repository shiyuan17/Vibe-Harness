---
name: define-goal
description: Use to write, refine, or activate `/goal` or execution/exploration objectives—not goal status operations.
---

# 定义可持续目标

先调查，再把意图写成 Agent 可持续追踪且可验证的目标。完整格式读取 [references/goal-contract.md](references/goal-contract.md)。

## 执行

1. 检查仓库、测试、文档和公开来源；实测命令与基线，无法验证时标为假设并放入任务 0。
2. 区分执行型与探索型；将指定技术手段视为待验证假设，优先表达用户结果。
3. 只询问无法发现且会改变目标的决定；每轮最多三个，提供推荐项、互斥选择和结果影响。
4. 高影响决定未关闭时返回阻塞草稿；低风险可逆选择可采用显式默认值并说明猜错代价。
5. 生成不超过 4000 字符的 Goal Brief；已有可选任务记录时只引用路径，不复制状态。

## 原生目标

- 草拟、评审或优化请求只返回目标书，不自动激活。
- 只有用户明确要求激活且宿主暴露目标操作时才先检查当前目标并调用；成功后才声称已激活。
- 未完成目标不得静默替换；缺少原生能力时返回可复制的 `/goal <内容>`，其他宿主返回普通提示词。
- 目标不扩大授权、写入范围、审批、凭据或完成证据边界。

## 禁止

- 不编造命令、基线、来源、原生能力或激活结果。
- 不通过降低测试、评测或验收标准制造完成。
- 不创建 `PROGRESS.md`、`BLOCKED.md` 或平行进度系统；优先复用宿主目标或现有可选任务记录。
