interface:
  display_name: "Define Goal"
  short_description: "Turn rough ideas into durable, verifiable goals"
  default_prompt: "Use $define-goal to turn this request into a decision-complete persistent goal or portable goal brief."
policy:
  allow_implicit_invocation: true
