---
name: api-and-interface-design
description: Use for public APIs, schemas, events, config, or consumed module boundaries.
---

# 设计接口契约

以现有契约和调用方为真值，先固定边界再实现。

常驻契约见 `docs/rules/api-rules.md`；本 Skill 是按需展开的执行步骤，两者描述同一契约，修改须同步。

## 执行

1. 找到契约来源、生产者、消费者和兼容窗口。
2. 明确输入、输出、错误、权限、可空性、分页、幂等、重试与并发冲突语义。
3. 在外部边界校验输入并限制大小与频率，保持内部类型可信；不要臆造字段或错误码。
4. 优先做可加的兼容演进；改名、改类型、改语义或收紧校验时设计迁移、弃用路径与回滚。
5. 同步 mapper、mock、文档和两侧契约测试。
6. 用真实调用路径证明生产者与消费者一致，不以单侧测试替代。

## 交付

报告契约来源、兼容影响、迁移方式、两侧验证和剩余未决项。
