interface:
  display_name: "Clarify Requirements"
  short_description: "Clarify only product decisions that change outcomes"
  default_prompt: "Use $clarify-requirements to resolve only the product decisions that truly block this request."
policy:
  allow_implicit_invocation: true
