# COGNIS-RENAME-050 Red Team 审查包

- 任务编号：COGNIS-RENAME-050
- 审查者：Parfit（独立 subagent `019f73ec-ec62-7da1-b50b-695c28713caf`）
- 审查对象：Cognis v0.5.0 重命名 diff、兼容迁移规格与本轮验证证据
- 审查时间：2026-07-18T15:04:10.4011426+08:00

## 审查范围

正确性与边界、安全与滥用、架构依赖、测试有效性、发布与回滚、治理合规。重点核对 legacy CLI/config/state/marker/env 兼容、upgrade 事务、rollback/uninstall、红区确认、机器输出、旧名审计和 eval reference。

## 问题列表

| 问题编号 | 严重度 | 状态 | 位置 | 触发方式 | 影响 | 最小修复方向 |
| --- | --- | --- | --- | --- | --- | --- |
| RT-001 | High | Closed | `runtime/hooks/lib/context.mjs`、`runtime/hooks/git-hook.mjs` | canonical 与 legacy 配置并存，canonical hook mode 为 off | Codex/Git Hook 可能绕过配置冲突门禁 | 共享冲突感知配置读取器并让 guarded 入口 fail-closed |
| RT-002 | High | Closed | `scripts/cognis.js` | canonical config 与 pre-v4 state 并存，普通 install 未带 upgrade | 状态可被标记为 v4 而旧资产未完整迁移 | 规划前检查 stateVersion/product 并要求显式 upgrade |
| RT-003 | Medium | Closed | `scripts/lib/install-state.js`、`scripts/cognis.js` | rollback 遇到用户修改文件并产生 skip | 部分动作提交后 install state 丢失 | 有 skip 时回滚整个事务并报告 retainedState |
| RT-004 | Medium | Closed | `scripts/lib/docs-validation.js` | 在兼容文件中加入活动态旧产品声明 | 文件级或宽泛行级豁免可漏报品牌漂移 | 使用历史目录豁免与文件级精确兼容字面量规则 |
| RT-005 | Medium | Closed | `evals/suites/cognis-core.json` | active eval 仍描述已移除的 apply 生命周期 | 发布评测未覆盖 Cognis upgrade/write/red-zone 合同 | 更新 EVAL-INSTALL-004 并显式批准新 reference |

## Medium 延期

无。所有 Medium finding 已关闭。

## 已核验证据

- 双配置 Codex/Git Hook 回归测试通过。
- canonical config + pre-v4 state 的 upgrade 门禁回归测试通过。
- rollback skip 原子恢复并保留 state 的回归测试通过。
- 旧名审计绕过样例被拒绝，文档治理测试 12/12 通过。
- EVAL-INSTALL-004 使用 `install --upgrade`、`--write`、`--confirm-red-zone`；suite/reference/result hash 为 `d46fbd2eccbce04f674ac32b90aa5eb062e001796e6ade2eb47f70588525673b`。
- reviewer 最终复核：剩余 Critical/High/Medium 为 0。

## 未覆盖审查轴与剩余风险

- GitHub 仓库改名、badge、artifact URL、本地目录名与 origin URL 属于发布后的仓库外人工步骤。
- 真实 POSIX symlink 行为依赖 CI 平台验证；本轮 Windows 与跨平台测试覆盖路径边界合同。
- Agentmemory 依赖审计仍有计划内 Moderate advisories，保持 preview，不构成本次发布阻断项。

## 结论

批准。
