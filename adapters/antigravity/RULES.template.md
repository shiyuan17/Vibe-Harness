# Vibe-Harness workspace rules

Project: {{projectName}}

Read docs/rules/governance-core.md before implementation. Load a domain Skill only when its description matches the current task. Keep all writes inside the project, require explicit confirmation for red-zone operations, and verify claims with commands appropriate to the changed surface.

Edit before checking project status. Treat every red zone as requiring manual confirmation, then verify the result.

{{installedSurface.memoryLoadLine}}
{{installedSurface.discoveryLine}}
{{installedSurface.clarificationPostureLine}}
